% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\PassOptionsToPackage{dvipsnames,svgnames,x11names}{xcolor}
\documentclass[
  11pt,
]{article}
\usepackage{xcolor}
\usepackage[margin=0.85in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage[]{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\AtBeginDocument{%
  \setlength{\parskip}{4.5pt plus 0.5pt}%
  \fancyfoot[L]{\footnotesize\color{Muted} Formal theory proof ledger -- theorem status shown per entry}%
  \fancyhead[R]{\small\sffamily\color{Muted} PROOF LEDGER}%
  \pretocmd{\section}{\Needspace{9\baselineskip}}{}{}%
}

% Render literal Lean audit output in the PDF.
\ifPDFTeX
\DeclareUnicodeCharacter{03B7}{\ensuremath{\eta}}
\DeclareUnicodeCharacter{03BA}{\ensuremath{\kappa}}
\DeclareUnicodeCharacter{03BC}{\ensuremath{\mu}}
\DeclareUnicodeCharacter{2115}{\ensuremath{\mathbb{N}}}
\DeclareUnicodeCharacter{211D}{\ensuremath{\mathbb{R}}}
\DeclareUnicodeCharacter{2191}{\ensuremath{\uparrow}}
\DeclareUnicodeCharacter{2192}{\ensuremath{\to}}
\DeclareUnicodeCharacter{21D1}{\ensuremath{\Uparrow}}
\DeclareUnicodeCharacter{2202}{\ensuremath{\partial}}
\DeclareUnicodeCharacter{2203}{\ensuremath{\exists}}
\DeclareUnicodeCharacter{2208}{\ensuremath{\in}}
\DeclareUnicodeCharacter{2227}{\ensuremath{\wedge}}
\DeclareUnicodeCharacter{222B}{\ensuremath{\int}}
\DeclareUnicodeCharacter{2264}{\ensuremath{\le}}
\DeclareUnicodeCharacter{22A4}{\ensuremath{\top}}
\DeclareUnicodeCharacter{22EF}{\ensuremath{\cdots}}
\fi

\ifPDFTeX
\DeclareUnicodeCharacter{2200}{\ensuremath{\forall}}
\DeclareUnicodeCharacter{1DA0}{\ensuremath{{}^{f}}}
\DeclareUnicodeCharacter{2194}{\ensuremath{\leftrightarrow}}
\DeclareUnicodeCharacter{2265}{\ensuremath{\ge}}
\fi

\ifPDFTeX
\DeclareUnicodeCharacter{27E8}{\ensuremath{\langle}}
\DeclareUnicodeCharacter{27E9}{\ensuremath{\rangle}}
\DeclareUnicodeCharacter{21D1}{\ensuremath{\Uparrow}}
\fi
\usepackage{amsmath,amssymb,mathtools}
\usepackage{microtype}
\usepackage{longtable,booktabs,array,etoolbox}
\usepackage{fancyhdr}
\usepackage{fvextra}
\usepackage{xurl}
\usepackage{titlesec}
\usepackage{needspace}
\definecolor{Ink}{HTML}{182D40}
\definecolor{Accent}{HTML}{176C78}
\definecolor{Muted}{HTML}{5D6871}
\titleformat{\section}{\Large\bfseries\color{Ink}}{}{0pt}{}
\titleformat{\subsection}{\large\bfseries\color{Accent}}{}{0pt}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{Ink}}{}{0pt}{}
\titlespacing*{\section}{0pt}{2.2ex plus 1ex minus .2ex}{1ex}
\titlespacing*{\subsection}{0pt}{1.8ex plus .6ex}{.7ex}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\sffamily\color{Muted} AIYAGARI / FORMAL THEORY}
\fancyhead[R]{\small\sffamily\color{Muted} ARCHITECTURE v1.0}
\fancyfoot[L]{\footnotesize\color{Muted} Design specification — not a completed Lean formalization}
\fancyfoot[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}
\setlength{\headheight}{15pt}
\setlength{\emergencystretch}{3em}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5.5pt plus 1pt}
\setlength{\LTpre}{8pt}
\setlength{\LTpost}{8pt}
\RecustomVerbatimEnvironment{verbatim}{Verbatim}{breaklines=true,fontsize=\small,breakanywhere=true}
\AtBeginEnvironment{longtable}{\small}
\widowpenalty=10000
\clubpenalty=10000
\ifPDFTeX
\DeclareUnicodeCharacter{2192}{\ensuremath{\to}}
\DeclareUnicodeCharacter{21A6}{\ensuremath{\mapsto}}
\DeclareUnicodeCharacter{00D7}{\ensuremath{\times}}
\fi
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={Aiyagari: Proof Ledger},
  colorlinks=true,
  linkcolor={Accent},
  filecolor={Maroon},
  citecolor={Accent},
  urlcolor={Accent},
  pdfcreator={LaTeX via pandoc}}

\title{Aiyagari: Proof Ledger}
\usepackage{etoolbox}
\makeatletter
\providecommand{\subtitle}[1]{% add subtitle to \maketitle
  \apptocmd{\@title}{\par {\large #1 \par}}{}{}
}
\makeatother
\subtitle{15 GREEN / 0 REVIEW\_READY / 42 UNFORMALIZED}
\author{}
\date{September 11, 2026 -\/- Version 1.0}

\begin{document}
\maketitle

\textbf{Economic status:} P01, P02, P03, H01, H02, H03, H04, H05, H07,
H08, H09, H10, H11, H12, H13 are \textbf{GREEN}; H06, H14, D01, D02,
D03, S01, S02, S03, S04, S05, S06, A01, A02, A03, A04, A05, N01, N02,
N03, N04, N05, N06, N07, B01, B02, B03, F01, F02, G01, G02, G03, G04,
G05, G06, G07, G08, NP01, NP02, NP03, E01, E02, E03 are
\textbf{UNFORMALIZED}. M00 bootstrap acceptance remains infrastructure
only. Exact acceptance records are in \texttt{reviews/}. Proposed proof
plans remain proposed until checked.

The completed ledger must replace each plan pointer with the actual
readable proof, exact elaborated Lean signature, all economic
hypotheses, axiom output and review evidence.

\section{Milestone 00 --- compiled API
infrastructure}\label{milestone-00-compiled-api-infrastructure}

\textbf{Status:} GREEN / accepted 2026-09-11. Scope:
bootstrap/environment/source/API infrastructure only, including
build/audit infrastructure and representation preflight. See
\texttt{reviews/00\_acceptance.md} for the external decision and four
reviewer qualifications. No economic adequacy is inferred from the
probes. No economic contract is assigned to this milestone and none is
promoted. Source bytes are verified; mathematical page locators below
remain the approved design locators. The clean Lean 4.32.0 / pinned
Mathlib build and audit evidence are in
\texttt{reports/00\_environment.md} and
\texttt{reports/logs/00\_axioms.log}.

\textbf{Actual economic assumptions:} none. The generic Banach result
assumes \texttt{ContractingWith\ K\ T}; the kernel results assume the
specified Markov instances and measurability; weak test convergence
assumes weak convergence; family compactness assumes uniform tightness.
These mathematical hypotheses are not household primitives or proved
economic conclusions.

\textbf{Readable proofs.} The bounded continuous function instance gives
completeness, and evaluation distance is at most the supremum distance.
NNReal inherits its complete, separable metric and Borel structure.
Banach supplies a fixed point and its uniqueness for a contraction on
the bounded-value space. The constant zero operator has Lipschitz
constant zero, strictly below one; uniqueness identifies its fixed point
as zero. This is a concrete infrastructure witness, not the primitive
economic consistency witness P03.

A bounded continuous real function is measurable and norm bounded, hence
integrable against any finite probability measure. A measurable
pushforward is again a probability law. The map-integrability
equivalence proves integrability of the composed test, and the
change-of-variables theorem gives the integral identity. The pushforward
probe exports both integrability statements. Markov composition
preserves probability mass; composing with the identity changes nothing.
A deterministic kernel transports a measure to its pushforward. These
are generic kernel facts, with no stationarity claim.

Weak convergence implies convergence of every bounded continuous test by
its installed characterization. On the compact interval {[}0,1{]}, the
probability-law space is compact. Every single law on NNReal is tight by
complete second-countable metrizability. Prokhorov gives compact closure
for a uniformly tight family. It does not provide an unbounded-moment
limit. Finally, a constant-infinity ENNReal function demonstrates that
the marginal representation can retain infinity at zero; it asserts no
derivative theorem.

\textbf{Exact elaborated declaration inventory and axiom output.} The
following is the actual successful \texttt{lake\ env\ lean\ Audit.lean}
output. Lean suppresses proof arguments with an ellipsis; the explicit
source signatures and proofs are in \texttt{Probes/Core.lean}. All
declarations pass \texttt{assert\_no\_sorry}; only the standard
foundational axioms occur. The abbreviations are State = NNReal,
ValueSpace = bounded continuous State-to-Real functions, and
CompactInterval = {[}0,1{]} as a subtype.

\begin{verbatim}
Aiyagari1994.Probes.State : Type
'Aiyagari1994.Probes.State' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.ValueSpace : Type
'Aiyagari1994.Probes.ValueSpace' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.CompactInterval : Set ℝ
'Aiyagari1994.Probes.CompactInterval' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.value_complete : CompleteSpace Aiyagari1994.Probes.ValueSpace
'Aiyagari1994.Probes.value_complete' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.sup_distance (f g : Aiyagari1994.Probes.ValueSpace) (z : Aiyagari1994.Probes.State) :
  dist (f z) (g z) ≤ dist f g
'Aiyagari1994.Probes.sup_distance' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.state_complete : CompleteSpace Aiyagari1994.Probes.State
'Aiyagari1994.Probes.state_complete' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.state_separable : TopologicalSpace.SeparableSpace Aiyagari1994.Probes.State
'Aiyagari1994.Probes.state_separable' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.state_borel : BorelSpace Aiyagari1994.Probes.State
'Aiyagari1994.Probes.state_borel' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.state_measurable_id : Measurable id
'Aiyagari1994.Probes.state_measurable_id' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.banach_on_values (T : Aiyagari1994.Probes.ValueSpace → Aiyagari1994.Probes.ValueSpace) (K : NNReal)
  (hT : ContractingWith K T) : ∃! v : Aiyagari1994.Probes.ValueSpace, T v = v
'Aiyagari1994.Probes.banach_on_values' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.zero_contraction : ContractingWith 0 fun (x : Aiyagari1994.Probes.ValueSpace) => 0
'Aiyagari1994.Probes.zero_contraction' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.zero_fixed_point :
  ContractingWith.fixedPoint (fun (x : Aiyagari1994.Probes.ValueSpace) => 0) Aiyagari1994.Probes.zero_contraction = 0
'Aiyagari1994.Probes.zero_fixed_point' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.bounded_test_integrable (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State)
  (f : Aiyagari1994.Probes.ValueSpace) : MeasureTheory.Integrable ⇑f ↑μ
'Aiyagari1994.Probes.bounded_test_integrable' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.pushforward_test (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State)
  (g : Aiyagari1994.Probes.State → Aiyagari1994.Probes.State) (hg : Measurable g) (f : Aiyagari1994.Probes.ValueSpace) :
  MeasureTheory.Integrable ⇑f ↑(μ.map ⋯) ∧
    MeasureTheory.Integrable (fun (x : Aiyagari1994.Probes.State) => f (g x)) ↑μ ∧
      ∫ (y : Aiyagari1994.Probes.State), f y ∂↑(μ.map ⋯) = ∫ (x : Aiyagari1994.Probes.State), f (g x) ∂↑μ
'Aiyagari1994.Probes.pushforward_test' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.kernel_composition
  (κ η : ProbabilityTheory.Kernel Aiyagari1994.Probes.State Aiyagari1994.Probes.State)
  [ProbabilityTheory.IsMarkovKernel κ] [ProbabilityTheory.IsMarkovKernel η] :
  ProbabilityTheory.IsMarkovKernel (η.comp κ)
'Aiyagari1994.Probes.kernel_composition' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.kernel_identity (κ : ProbabilityTheory.Kernel Aiyagari1994.Probes.State Aiyagari1994.Probes.State) :
  κ.comp ProbabilityTheory.Kernel.id = κ
'Aiyagari1994.Probes.kernel_identity' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.kernel_transport (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State)
  (g : Aiyagari1994.Probes.State → Aiyagari1994.Probes.State) (hg : Measurable g) :
  (↑μ).bind ⇑(ProbabilityTheory.Kernel.deterministic g hg) = ↑(μ.map ⋯)
'Aiyagari1994.Probes.kernel_transport' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.deterministic_markov (g : Aiyagari1994.Probes.State → Aiyagari1994.Probes.State)
  (hg : Measurable g) : ProbabilityTheory.IsMarkovKernel (ProbabilityTheory.Kernel.deterministic g hg)
'Aiyagari1994.Probes.deterministic_markov' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.transport_mass (κ : ProbabilityTheory.Kernel Aiyagari1994.Probes.State Aiyagari1994.Probes.State)
  [ProbabilityTheory.IsMarkovKernel κ] (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State) :
  ((↑μ).bind ⇑κ) Set.univ = 1
'Aiyagari1994.Probes.transport_mass' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.weak_test_convergence (μs : ℕ → MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State)
  (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State) (h : Filter.Tendsto μs Filter.atTop (nhds μ))
  (f : Aiyagari1994.Probes.ValueSpace) :
  Filter.Tendsto (fun (n : ℕ) => ∫ (x : Aiyagari1994.Probes.State), f x ∂↑(μs n)) Filter.atTop
    (nhds (∫ (x : Aiyagari1994.Probes.State), f x ∂↑μ))
'Aiyagari1994.Probes.weak_test_convergence' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.compact_interval_laws :
  CompactSpace (MeasureTheory.ProbabilityMeasure ↑Aiyagari1994.Probes.CompactInterval)
'Aiyagari1994.Probes.compact_interval_laws' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.individual_law_tight (μ : MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State) :
  MeasureTheory.IsTightMeasureSet {↑μ}
'Aiyagari1994.Probes.individual_law_tight' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.tight_family_compact (S : Set (MeasureTheory.ProbabilityMeasure Aiyagari1994.Probes.State))
  (hS : MeasureTheory.IsTightMeasureSet {x : MeasureTheory.Measure Aiyagari1994.Probes.State | ∃ μ ∈ S, ↑μ = x}) :
  IsCompact (closure S)
'Aiyagari1994.Probes.tight_family_compact' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.Probes.extended_zero_value : ∃ (q : Aiyagari1994.Probes.State → ENNReal), q 0 = ⊤
'Aiyagari1994.Probes.extended_zero_value' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{M00 review boundary:} the bootstrap acceptance certifies only
its stated infrastructure scope. M01 was separately assigned and its
results follow below; generic API success does not certify the later
economic proofs.

\section{P01 --- Shifted budget
normalization}\label{p01-shifted-budget-normalization}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Milestone:} 01.

\textbf{Target:} \texttt{Aiyagari1994.shifted\_budget\_iff} in
\texttt{Aiyagari1994/Budget/Normalization.lean}.

\textbf{Actual statement and assumptions.} For arbitrary real net rate,
wage, current labor, debt shift, current net assets, next net assets and
consumption, the original budget equality together with the next
borrowing inequality is equivalent to the shifted budget equality
together with nonnegative next shifted assets. There are no economic
premises: this algebraic result is stronger than its restriction to
admissible prices. Gross return is structurally \(1+r\); it is never
independent data alongside \(r\) in the original-price structure.

\textbf{Readable proof.} Expand \((1+r)(a+\phi)\) and cancel \(r\phi\).
Relative to the original equality, both sides acquire exactly \(\phi\).
Adding or subtracting \(\phi\) also proves \(-\phi\leq a_{next}\) iff
\(0\leq a_{next}+\phi\). The main theorem joins these two genuine
equivalences. Consumption is the same real number in both systems, so a
separate nonnegativity restriction on consumption is preserved without
an additional premise in the algebraic identity.

The audited supporting declarations keep the components separate:

\begin{itemize}
\tightlist
\item
  \texttt{shifted\_budget\_equality\_iff}: the budget equality
  component.
\item
  \texttt{shifted\_borrowing\_iff}: the borrowing inequality component.
\item
  \texttt{next\_resource\_identity}: next labor and shifted savings in
  the next-period resource equation.
\item
  \texttt{original\_normalized\_coordinates}: the constructed map has
  \(R=1+r\) and \(k=-r\phi\).
\end{itemize}

\textbf{Source correspondence.} A94 equations (1b), (3a)-(4b), printed
pp.~665-666 / PDF pp.~8-9, visually inspected in M01. No No-Ponzi
equivalence or policy theorem is claimed.

\textbf{Exact elaborated signature and transitive axioms:}

\begin{verbatim}
Aiyagari1994.shifted_budget_iff (r w l phi a a_next c : ℝ) :
  c + a_next = (1 + r) * a + w * l ∧ -phi ≤ a_next ↔
    c + (a_next + phi) = (1 + r) * (a + phi) + w * l - r * phi ∧ 0 ≤ a_next + phi
'Aiyagari1994.shifted_budget_iff' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Integrability:} none; this is pathwise algebra.
\textbf{Dependencies:} primitive real arithmetic only. \textbf{Adequacy
caveat:} externally accepted in \texttt{reviews/01\_acceptance.md}; no
No-Ponzi claim is included.

\section{P02 --- Effective borrowing
limits}\label{p02-effective-borrowing-limits}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Milestone:} 01.

\textbf{Target:}
\texttt{Aiyagari1994.effectiveLimit\_admissible\_continuous}.

\textbf{Module:} \texttt{Aiyagari1994/Budget/EffectiveLimit.lean}.

\textbf{Actual assumptions.} The cap \(b\) is a fixed nonnegative real
and the labor floor \(l_{min}\) is strictly positive. Scalar
admissibility quantifies over positive wages and labor at least the
floor. Finite-cap continuity is joint in \((w,r)\) on \(w>0,r>-1\), for
each fixed \(b,l_{min}\). Its nonnegativity and effective-income
inequalities are actually proved for every real rate. The original-price
constructor additionally requires \(r>-1\) to ensure positive gross
return. The natural branch separately requires \(r>0\) and has no
finite-cap premise.

\textbf{Readable proof.} At \(r>0\), both entries of
\(\min(b,w l_{min}/r)\) are nonnegative, and
\(r\phi\leq w l_{min}\leq w l\). At \(r\leq0\), \(\phi=b\geq0\) and
\(r b\leq0<w l_{min}\leq w l\). Thus effective income is nonnegative in
each branch.

For continuity, the proof establishes the exact auxiliary identity

\[\phi(b,l_{min},w,r)=\frac{b(w l_{min})}{\max(w l_{min},br)}.\]

When \(br\leq w l_{min}\) the expression equals \(b\); otherwise the
positive-rate minimum equals \(w l_{min}/r\). The denominator is at
least \(w l_{min}>0\). Continuity of multiplication, maximum and
division therefore proves joint continuity on the stated price domain.
The case \(b=0\) never requires division by \(b\).

The explicit \texttt{effectiveLimit\_zero\_neighborhood} theorem also
proves the source-sensitive boundary step: the continuous expression
\(w' l_{min}-b r'\) is positive near \((w,0)\). Consequently, if
\(r'>0\), then \(b\leq w'l_{min}/r'\); the minimum equals \(b\). For
nearby nonpositive rates the definition is already \(b\). This proves
local constancy at zero, not a reliance on a convention for division by
zero.

For the separate natural limit \(\phi_{nat}=w l_{min}/r\), cancellation
with \(r>0\) gives \(-r\phi_{nat}=-w l_{min}\). Effective income is
exactly \(w(l-l_{min})\geq0\). Raw natural-limit continuity is proved
only on positive rates. No continuation of the raw debt limit through
zero is claimed.

\texttt{finiteCapPrices} and \texttt{naturalCapPrices} construct the
same \texttt{OriginalPrices} type with these proved admissibility facts.
\texttt{OriginalPrices.normalized} constructs normalized prices;
\texttt{NormalizedPrices.nextResources} returns NNReal using nonnegative
shifted savings and nonnegative effective income. The audited helper
\texttt{generated\_resources\_admissible} gives the explicit
generated-state inequality. No invariant or absorbing bound is used.

\textbf{Source correspondence.} A94 equations (2a)-(2b), printed p.~666
/ PDF p.~9, visually inspected in M01. The explicit continuity argument
and the rational/max helper are implementation mathematics consistent
with architecture section 2.

\textbf{Exact elaborated signature and transitive axioms:}

\begin{verbatim}
Aiyagari1994.effectiveLimit_admissible_continuous (b lo : ℝ) (hb : 0 ≤ b) (hl : 0 < lo) :
  (∀ (w r : ℝ), 0 < w → 0 ≤ Aiyagari1994.effectiveLimit b lo w r) ∧
    (∀ (w r l : ℝ), 0 < w → lo ≤ l → 0 ≤ w * l - r * Aiyagari1994.effectiveLimit b lo w r) ∧
      ContinuousOn (fun (p : ℝ × ℝ) => Aiyagari1994.effectiveLimit b lo p.1 p.2) {p : ℝ × ℝ | 0 < p.1 ∧ -1 < p.2} ∧
        (∀ (w : ℝ),
            0 < w →
              ∀ᶠ (p : ℝ × ℝ) in nhds (w, 0),
                (0 < p.2 → b ≤ p.1 * lo / p.2) ∧ Aiyagari1994.effectiveLimit b lo p.1 p.2 = b) ∧
          (∀ (w r : ℝ),
              0 < w →
                0 < r →
                  0 ≤ Aiyagari1994.naturalLimit lo w r ∧
                    -r * Aiyagari1994.naturalLimit lo w r = -w * lo ∧
                      ∀ (l : ℝ),
                        lo ≤ l →
                          w * l - r * Aiyagari1994.naturalLimit lo w r = w * (l - lo) ∧
                            0 ≤ w * l - r * Aiyagari1994.naturalLimit lo w r) ∧
            ContinuousOn (fun (p : ℝ × ℝ) => Aiyagari1994.naturalLimit lo p.1 p.2) {p : ℝ × ℝ | 0 < p.1 ∧ 0 < p.2}
'Aiyagari1994.effectiveLimit_admissible_continuous' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Integrability:} none; scalar budget/admissibility results.
\textbf{Dependencies:} primitive real order, algebra and topology; P01
supplies the normalized-price context. \textbf{Adequacy caveat:} the
quantified finite and natural clauses are independent branches. The
statement does not require a household simultaneously to have both
borrowing rules. External M01 review accepted this scope on 2026-09-11.

\section{P03 --- Exact primitive consistency
witness}\label{p03-exact-primitive-consistency-witness}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Milestone:} 01.

\textbf{Target:} \texttt{Aiyagari1994.corePrimitives\_nonempty} in
\texttt{Aiyagari1994/Primitives/Examples.lean}.

\textbf{Actual assumptions.} None are assumed by the existential
theorem. It constructs an element of the same
\texttt{HouseholdPrimitives} type used for arbitrary compact income
laws, together with \texttt{CoreRegularity}. The exact output data are
\(\beta=1/2\), \(R=1\), \(w=1\), intercept zero, original rate zero, cap
zero, debt limit zero, utility \(U(c)=c/(1+c)\), and the law placing
mass \(1/2\) at each labor endpoint \(1/2\) and \(3/2\).
Assets/resources remain NNReal. The finite shock distribution is only a
witness, not a restriction on the model family.

\textbf{Readable utility proof.} The utility is a real-valued function
by its declared type. On \(c\geq0\), its denominator is positive,
quotient continuity applies, and \(0\leq U(c)<1\), so it is bounded
above and below. Quotient differentiation proves

\[U'(c)=\frac{1}{(1+c)^2},\qquad U''(c)=-\frac{2}{(1+c)^3}.\]

The first derivative formula is proved for \(c\geq0\) and the second for
\(c>0\). To differentiate the derivative, the proof first establishes
the quotient's derivative formula on a neighborhood of each positive
\(c\), then transfers the derivative through that local equality.
Positive first derivative and negative second derivative on the
interior, together with continuity at zero, give strict increase and
strict concavity on \([0,\infty)\). Quotient smoothness supplies C1/C2
(indeed every finite order) on \((0,\infty)\). Algebra gives

\[-cU''(c)/U'(c)=2c/(1+c)\leq2.\]

The curvature record uses threshold one and finite bound two. There is
no assumption of global continuity or concavity across the singularity
at \(c=-1\), and the general primitive records require neither finite
nor infinite marginal utility at zero.

\textbf{Readable income proof and integrability.}
\texttt{witnessMeasure} is the sum of two half-weighted Dirac measures
on the full subtype \([1/2,3/2]\). Its total mass is one. Each endpoint
singleton has mass exactly one half; the complement of the two endpoints
has zero mass. Every relative endpoint neighborhood contains the
corresponding singleton and therefore has positive mass. These facts
also prove the exact topological support and nondegeneracy.

\texttt{labor\_integrable} proves labor integrability for every
\texttt{IncomeData} law from continuity on the compact labor subtype and
finiteness of the probability law. The direct mean computation
separately checks integrability for each weighted Dirac term before
using integral additivity, giving \((1/2)(1/2)+(1/2)(3/2)=1\).

\texttt{historyLaw} is the finite product of copies of the general labor
law. For every finite horizon it is a probability measure, its
coordinate pushforwards are the original law, its coordinates are
independent, and the \texttt{piFinSuccAbove} measurable equivalence
gives the current-draw/tail product decomposition. The same proved
\texttt{history\_probability}, \texttt{history\_marginal},
\texttt{history\_independent} and \texttt{history\_step} declarations
apply directly to \texttt{witnessIncome}; no IID conclusion is stored as
an assumed primitive field. Lifetime Bellman verification and
infinite-path construction remain later work.

Finally, \texttt{witnessModel} combines the utility, income and the
normalized map of the finite-cap original prices.
\texttt{witness\_core\_regular} supplies every required regularity
proof. The main theorem packages that actual inhabitant and checks the
exact parameter identities.

\textbf{Source correspondence.} New exact consistency witness specified
by architecture section 2 and the accepted M01 instructions; not claimed
to appear in the papers. A93 is the manifest source key for the modeled
primitive context. The budget/income conventions match A94 printed
pp.~665-666 / PDF pp.~8-9, inspected visually.

\textbf{Exact elaborated signature and transitive axioms:}

\begin{verbatim}
Aiyagari1994.corePrimitives_nonempty :
  ∃ (m : Aiyagari1994.HouseholdPrimitives),
    Aiyagari1994.CoreRegularity m ∧
      m.beta = 1 / 2 ∧
        m.prices.grossReturn = 1 ∧
          m.prices.wage = 1 ∧
            m.prices.intercept = 0 ∧
              m.utility = Aiyagari1994.witnessUtility ∧
                m.income = Aiyagari1994.witnessIncome ∧
                  ∃ (p : Aiyagari1994.OriginalPrices m.income),
                    p.normalized = m.prices ∧
                      p.netRate = 0 ∧
                        p.wage = 1 ∧ p.debtLimit = 0 ∧ p.debtLimit = Aiyagari1994.effectiveLimit 0 m.income.lower 1 0
'Aiyagari1994.corePrimitives_nonempty' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Primitive audit.} All twelve structures are printed in the
direct audit log. Their fields are utility data and authorized
regularity, support/probability data, optional mean normalization,
prices/admissibility, and discount data. No value function, policy,
Bellman theorem, Euler/envelope equation, Feller/mixing condition,
invariant distribution, asset bound, asset supply or equilibrium
conclusion is a field. \texttt{HouseholdPrimitives} imposes \(R>0\) and
never \(\beta R<1\).

\textbf{Adequacy caveat:} consistency of primitives does not prove
optimality, stationarity, equilibrium or any later contract. P03 is
GREEN by external acceptance on 2026-09-11, within this
primitive-consistency scope. Helpers and exact source files are
inventoried in \texttt{reports/01\_declarations.json}.

\section{H01 --- Bellman self-map and
contraction}\label{h01-bellman-self-map-and-contraction}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Review gate:} M02A. See
\texttt{reviews/02a\_acceptance.md}.

\textbf{Target:} \texttt{Aiyagari1994.bellman\_selfmap\_contracting} in
\texttt{Aiyagari1994/Household/Bellman.lean}.

\textbf{Actual assumptions.} Only \texttt{HouseholdPrimitives}. The
construction uses \(0<\beta<1\), utility continuity and boundedness on
nonnegative consumption, the finite probability law on the compact labor
subtype, and nonnegative affine generated resources. Strict utility
increase and concavity are available in BASIC but not needed for H01. No
return-discount restriction is imposed: every admissible \(R>0\) is
covered.

\textbf{Definition and integrability.} For \(a\in[0,z]\),

\[Q_v(z,a)=U(z-a)+\beta\int v(Ra+e(l))\,d\nu(l).\]

The transition and integrand facts are proved separately:

\begin{itemize}
\tightlist
\item
  \texttt{nextResources\_continuous}: joint transition continuity.
\item
  \texttt{continuation\_integrand\_continuous}: joint integrand
  continuity.
\item
  \texttt{continuation\_integrand\_bound}: the bound by
  \(\|v\|_\infty\).
\end{itemize}

\texttt{continuation\_integrable} proves integrability for every action
by continuity on the compact labor subtype and finiteness of its
probability measure. \texttt{continuation\_continuous} applies the
installed compact parametric integral theorem on the whole labor
subtype. Integral subtraction in the contraction proof uses the two
explicit integrability results.

The objective has a continuous extension using NNReal subtraction
outside the feasible set. \texttt{bellmanObjective\_feasible} identifies
it with the displayed real-subtraction formula whenever \(a\leq z\).
Thus no negative-consumption utility value enters the maximization.
Joint objective continuity is proved by composing primitive utility
continuity with nonnegative consumption and adding the continuous
continuation term.

\textbf{Maximum and boundedness proof.} The actual action is shifted
savings \(a\). For maximum-value continuity only,
\texttt{shareObjective} evaluates \(a=tz\) on the fixed compact subtype
\(t\in[0,1]\). \texttt{share\_feasible} and \texttt{feasible\_share}
prove that this parametrization covers exactly \([0,z]\); at zero, all
shares give the same singleton action. \texttt{compactMax\_continuous}
wraps the installed compact supremum theorem;
\texttt{compactMax\_attained}, \texttt{le\_compactMax} and
\texttt{compactMax\_le} give attainment and bounds. These are reusable
generic theorems in \texttt{Analysis/ParametricMax.lean}, with their
mathematical hypotheses discharged by
\texttt{shareObjective\_continuous}.

For a primitive bound \(|U(c)|\leq C\) and bounded continuous \(v\),

\[|Q_v(z,a)|\leq C+\beta\|v\|_\infty.\]

\texttt{bellmanValue\_bound} transfers the bound through an attained
optimizer. The distance between any two Bellman values is consequently
bounded by twice this constant, supplying the bounded-continuous
constructor. There is no bound on \(z\), assets or the state space.

\textbf{Contraction proof.} The probability mass is one, so
\texttt{continuation\_sub\_bound} gives
\(|E[v(Ra+e)]-E[w(Ra+e)]|\leq\|v-w\|_\infty\). Current utility cancels
at the same action, giving \texttt{objective\_sub\_bound}. Take an
optimizer for \(v\) at \(z\). Its objective under \(w\) is no greater
than the maximum for \(w\), hence
\(T v(z)-T w(z)\leq\beta\|v-w\|_\infty\). Reverse the candidates and use
symmetry of the norm to obtain the absolute-value bound.
\texttt{bellman\_pointwise\_contraction} and
\texttt{bellman\_norm\_contraction} turn this into the supremum-norm
bound, then the installed \texttt{ContractingWith} type. No subtraction
is moved through a maximum.

\textbf{Exact elaborated anchor and canonical self-map:}

\begin{verbatim}
Aiyagari1994.bellman_selfmap_contracting (m : Aiyagari1994.HouseholdPrimitives) :
  ContractingWith ⟨m.beta, ⋯⟩ (Aiyagari1994.bellmanOperator m)
'Aiyagari1994.bellman_selfmap_contracting' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.bellmanOperator (m : Aiyagari1994.HouseholdPrimitives) (v : Aiyagari1994.ValueSpace) :
  Aiyagari1994.ValueSpace
'Aiyagari1994.bellmanOperator' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Source correspondence:} A94 equations (4)-(7), printed
pp.~666-667 / PDF pp.~9-10, visually inspected. A93 Appendix Proposition
2, printed pp.~37-38 / PDF pp.~38-39, is the manifest context and was
also inspected. Its differentiability and envelope claims are outside
M02A; the BASIC Bellman construction is explicitly reconstructed under
architecture section 3.

\textbf{Dependencies and qualification.} P01 supplies the established
shifted-budget interpretation; the normalized-price type is unchanged.
The kernel-checked result is a Bellman self-map, not lifetime
verification or an equilibrium result. All H01 exports pass direct
no-sorry and transitive-axiom audits; only the accepted foundational
axioms occur.

\section{H02 --- Canonical bounded continuous value
function}\label{h02-canonical-bounded-continuous-value-function}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Review gate:} M02A. See
\texttt{reviews/02a\_acceptance.md}.

\textbf{Target:}
\texttt{Aiyagari1994.valueFunction\_unique\_fixedPoint}.

\textbf{Module:} \texttt{Aiyagari1994/Household/Value.lean}.

\textbf{Actual assumptions and dependencies.} Only
\texttt{HouseholdPrimitives}, using H01. The bounded-continuous real
function space on all NNReal is complete. No value function is supplied
as a premise, and no extra economic assumption is introduced.

\textbf{Fixed-point and convergence proof.} Define
\texttt{valueFunction\ m} once using the installed
\texttt{ContractingWith.fixedPoint} applied to
\texttt{bellmanOperator\ m} and
\texttt{bellman\_selfmap\_contracting\ m}. The installed fixed-point and
uniqueness theorems prove \texttt{valueFunction\_fixedPoint} and
\texttt{valueFunction\_unique} among all bounded continuous candidates.
\texttt{valueIteration\_tendsto} gives convergence in the supremum
metric from every initial bounded continuous function.
\texttt{valueIteration\_uniform} uses the installed equivalence with
uniform convergence, not merely a pointwise assertion. Later modules use
exactly this canonical definition.

\textbf{Exact range bounds.} \texttt{utilityRange} is the image of all
nonnegative real consumption under \(U\).
\texttt{utilityRange\_nonempty} witnesses consumption zero.
\texttt{utilityRange\_bounded} derives both real bounds from the
primitive absolute bound. Define

\[U_{inf}=\operatorname{sInf}(\operatorname{range}U),\qquad
U_{sup}=\operatorname{sSup}(\operatorname{range}U).\]

The functions here have NNReal consumption arguments coerced to real.
Thus these are exactly the finite real infimum and supremum over
\(c\geq0\). \texttt{utility\_bounds} supplies the order inequalities.
Likewise \texttt{valueRange\_bounded} bounds the range of any bounded
continuous candidate by its norm.

Let \(L=\inf V\) and \(H=\sup V\). Explicit integrability and integral
monotonicity imply \(L\leq E[V(Ra+e)]\leq H\)
(\texttt{continuation\_range\_bounds}). The feasible action zero gives
\(V(z)\geq U_{inf}+\beta L\); an attained optimizer gives
\(V(z)\leq U_{sup}+\beta H\). Taking infimum and supremum, respectively,
yields

\[U_{inf}+\beta L\leq L,\qquad H\leq U_{sup}+\beta H.\]

Since \(1-\beta>0\), division and \(L\leq V(z)\leq H\) give exactly

\[\frac{U_{inf}}{1-\beta}\leq V(z)\leq\frac{U_{sup}}{1-\beta}.\]

No attainment of the infimum or supremum of \(V\) on the unbounded state
space is assumed. This argument uses conditional-completeness lemmas
with proved nonemptiness and boundedness; it does not replace the bounds
by an arbitrary symmetric constant.

\textbf{Exact elaborated anchor and canonical definition type:}

\begin{verbatim}
Aiyagari1994.valueFunction_unique_fixedPoint (m : Aiyagari1994.HouseholdPrimitives) :
  Aiyagari1994.bellmanOperator m (Aiyagari1994.valueFunction m) = Aiyagari1994.valueFunction m ∧
    (∀ (v : Aiyagari1994.ValueSpace), Aiyagari1994.bellmanOperator m v = v → v = Aiyagari1994.valueFunction m) ∧
      (∀ (v : Aiyagari1994.ValueSpace),
          TendstoUniformly (fun n z => ((Aiyagari1994.bellmanOperator m)^[n] v) z) (⇑(Aiyagari1994.valueFunction m))
            Filter.atTop) ∧
        ∀ (z : Aiyagari1994.Resources),
          Aiyagari1994.utilityInf m / (1 - m.beta) ≤ (Aiyagari1994.valueFunction m) z ∧
            (Aiyagari1994.valueFunction m) z ≤ Aiyagari1994.utilitySup m / (1 - m.beta)
'Aiyagari1994.valueFunction_unique_fixedPoint' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.valueFunction (m : Aiyagari1994.HouseholdPrimitives) : Aiyagari1994.ValueSpace
'Aiyagari1994.valueFunction' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Source correspondence:} A94 equations (4)-(7), printed
pp.~666-667 / PDF pp.~9-10, visually inspected. A93 Appendix Proposition
2, printed pp.~37-38 / PDF pp.~38-39, is the manifest context and was
also inspected. Its differentiability and envelope claims are outside
M02A; the BASIC Bellman construction is explicitly reconstructed under
architecture section 3.

\textbf{Qualification.} The canonical function is the unique bounded
continuous Bellman fixed point. Its interpretation as lifetime-optimal
expected utility remains H05. Audit results contain only
\texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}.

\section{H03 --- Value concavity and strict
increase}\label{h03-value-concavity-and-strict-increase}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Review gate:} M02A. See
\texttt{reviews/02a\_acceptance.md}.

\textbf{Target:}
\texttt{Aiyagari1994.valueFunction\_concave\_strictMono}.

\textbf{Module:} \texttt{Aiyagari1994/Household/Value.lean}.

\textbf{Actual assumptions and dependencies.} Only BASIC, through the
canonical H02 value function, primitive strict concavity and strict
monotonicity of utility, and the affine nonnegative transition. No
differentiability, IID histories, income nondegeneracy or impatience
condition is used.

\textbf{Ordinary concavity.} \texttt{NNRealConcave} quantifies over all
states \(x,y\) and all nonnegative real weights \(a,b\) summing to one,
represented as NNReal. It asserts \(a f(x)+b f(y)\leq f(ax+by)\).
\texttt{NNRealConcave.real\_combination} explicitly yields the usual
formula with any real \(\theta\in[0,1]\). This avoids imposing a real
vector-space instance on NNReal without weakening real
convex-combination concavity.

\textbf{Preservation proof.} Given feasible savings \(s\leq x\) and
\(t\leq y\), \(as+bt\leq ax+by\). Consumption at the combined pair is
\(a(x-s)+b(y-t)\). Primitive strict concavity supplies ordinary utility
concavity. \texttt{transition\_convex\_combination} proves the affine
identity for next resources, including the constant income term by
\(a+b=1\). Concavity of \(v\) therefore gives a pointwise continuation
inequality. \texttt{continuation\_concave} integrates it with explicit
integrability of each summand and uses integral addition and scalar
multiplication. Multiplication by positive beta and addition of the
utility inequality prove \texttt{objective\_convex\_combination}.

Take attained optimizers at the two original states. Their convex
combination is feasible at the combined state and cannot exceed that
state's maximum. This proves \texttt{bellman\_preserves\_concavity}.

\textbf{Iteration and limit.} Zero is concave and weakly increasing
(\texttt{nnrealConcave\_zero}, \texttt{monotone\_const}). Induction
gives \texttt{valueIteration\_concave} and
\texttt{valueIteration\_monotone} for zero-start iterates. The uniform
convergence established in H02 supplies the convergent evaluations at
each of the three states in the concavity inequality, and at both states
in the monotonicity inequality. Passing inequalities through these
proved limits establishes \texttt{valueFunction\_concave} and
\texttt{valueFunction\_monotone}. No unproved limit principle is used.

\textbf{Strict increase.} For \(x<y\), take an optimizer at \(x\) and
keep the same saving action at \(y\). It remains feasible. Current
consumption strictly increases; the continuation term is exactly
unchanged. Strict utility monotonicity makes the new feasible objective
strictly exceed the old maximum. Therefore \(T v\) is strictly
increasing for every candidate \(v\) (\texttt{bellman\_strictMono}), a
slightly stronger intermediate result than monotonicity preservation.
Applying the fixed-point identity proves
\texttt{valueFunction\_strictMono}. No envelope theorem or derivative is
involved.

\textbf{Exact elaborated anchor and real concavity formula:}

\begin{verbatim}
Aiyagari1994.valueFunction_concave_strictMono (m : Aiyagari1994.HouseholdPrimitives) :
  Aiyagari1994.NNRealConcave ⇑(Aiyagari1994.valueFunction m) ∧ StrictMono ⇑(Aiyagari1994.valueFunction m)
'Aiyagari1994.valueFunction_concave_strictMono' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.NNRealConcave.real_combination {f : Aiyagari1994.Resources → ℝ} (hf : Aiyagari1994.NNRealConcave f)
  (x y : Aiyagari1994.Resources) (theta : ℝ) (h0 : 0 ≤ theta) (h1 : theta ≤ 1) :
  theta * f x + (1 - theta) * f y ≤ f ⟨theta * ↑x + (1 - theta) * ↑y, ⋯⟩
'Aiyagari1994.NNRealConcave.real_combination' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Source correspondence:} A94 equations (4)-(7), printed
pp.~666-667 / PDF pp.~9-10, visually inspected. A93 Appendix Proposition
2, printed pp.~37-38 / PDF pp.~38-39, is the manifest context and was
also inspected. Its differentiability and envelope claims are outside
M02A; the BASIC Bellman construction is explicitly reconstructed under
architecture section 3.

\textbf{Qualification.} This is concavity and strict increase of the
canonical value function only. Policy monotonicity, differentiability
and drift remain later contracts. The no-sorry and transitive-axiom
audits pass with the accepted foundational axioms.

\section{H04 --- Unique continuous shifted-asset
policy}\label{h04-unique-continuous-shifted-asset-policy}

\textbf{Status:} GREEN, externally accepted 2026-09-11. \textbf{Scope:}
core. \textbf{Review gate:} M02A. See
\texttt{reviews/02a\_acceptance.md}.

\textbf{Target:} \texttt{Aiyagari1994.assetPolicy\_unique\_continuous}.

\textbf{Module:} \texttt{Aiyagari1994/Household/Policy.lean}.

\textbf{Actual assumptions and dependencies.} Only BASIC and H03's
canonical concave value function. In particular, there is no supplied
optimizer/policy, derivative, Euler condition, positive-consumption
premise, finite labor law, bounded asset space or invariant law.

\textbf{Existence and uniqueness.} \texttt{AssetOptimal\ m\ z\ a} means
\(a\leq z\) and \(Q_V(z,a)=V(z)\); \(a\) is NNReal. H01 attainment and
the H02 fixed-point equation prove \texttt{assetOptimal\_exists}. For
two distinct feasible actions, consumption \(z-a\) is distinct.
\texttt{objective\_strictConcave\_assets} proves strict concavity for
every positive pair of real convex weights by adding strict current
utility concavity and weak expected continuation concavity. Combined
actions remain feasible.

At zero all feasible actions equal zero. Otherwise two distinct
maximizing actions would give a midpoint with objective strictly above
their common maximum, a contradiction. This proves
\texttt{assetOptimal\_unique} and then
\texttt{assetOptimal\_existsUnique}. Classical choice from this proved
unique existence defines the canonical \texttt{assetPolicy}. It is never
a primitive field. \texttt{assetPolicy\_optimal} and
\texttt{assetPolicy\_le\_state} give maximization and feasibility.
\texttt{assetOptimal\_iff\_maximizes} proves equivalence with
maximization over every feasible shifted asset;
\texttt{assetPolicy\_existsUnique\_maximizer} exposes this as genuine
unique existence for use by H05.

\textbf{Reusable continuity lemma:}
\texttt{compact\_unique\_argmax\_continuous}.

\textbf{Analysis module:} \texttt{Analysis/ParametricMax.lean}.

\textbf{Continuity proof.} The reusable lemma uses a closed-graph proof.
For a closed choice set \(S\), intersect the graph
\(f(x,k)=\max_j f(x,j)\) with \(X\times S\). It is closed by joint
objective and maximum-value continuity. Projection to \(X\) is closed
because the choice space is compact. Uniqueness identifies this
projection with the inverse image of \(S\) under the selected optimizer;
hence the optimizer is continuous.

Apply this theorem to the share optimizer only on the open subtype of
strictly positive resources. Define \texttt{positiveAssetShare} as
\(A(z)/z\); feasibility puts it in \([0,1]\).
\texttt{positiveAssetShare\_mul} recovers the actual asset choice. At
positive \(z\), multiplication by \(z\) is injective, so uniqueness of
assets implies uniqueness of the share. Joint share-objective continuity
supplies the remaining generic hypothesis. Multiplying the continuous
share by \(z\) gives \texttt{assetPolicy\_continuous\_positive}.

At zero, \texttt{assetPolicy\_zero} and \(0\leq A(z)\leq z\) imply
convergence to zero by squeezing. Joining this boundary argument with
continuity on the open positive domain proves
\texttt{assetPolicy\_continuous}. No unique share at zero is asserted.
Continuity of \(V\) alone is never used as a policy-continuity argument.

\textbf{Consumption and forward interface.} Define
\texttt{consumptionPolicy\ m\ z\ =\ z\ -\ assetPolicy\ m\ z} in NNReal.
The proven asset bound gives its exact real-subtraction identity,
nonnegativity, and \(c(z)+A(z)=z\) (\texttt{consumptionPolicy\_coe},
\texttt{consumptionPolicy\_budget}). It is continuous by subtraction.
The main theorem exposes canonical uniqueness, feasibility, domination
of every feasible objective, budget and continuity.

\textbf{Exact elaborated anchor and maximization interface:}

\begin{verbatim}
Aiyagari1994.assetPolicy_unique_continuous (m : Aiyagari1994.HouseholdPrimitives) :
  (∀ (z : Aiyagari1994.Resources),
      Aiyagari1994.AssetOptimal m z (Aiyagari1994.assetPolicy m z) ∧
        ∀ (a : Aiyagari1994.Resources), Aiyagari1994.AssetOptimal m z a → a = Aiyagari1994.assetPolicy m z) ∧
    Continuous (Aiyagari1994.assetPolicy m) ∧
      (∀ (z a : Aiyagari1994.Resources),
          a ≤ z →
            Aiyagari1994.bellmanObjective m (Aiyagari1994.valueFunction m) z a ≤
              Aiyagari1994.bellmanObjective m (Aiyagari1994.valueFunction m) z (Aiyagari1994.assetPolicy m z)) ∧
        (∀ (z : Aiyagari1994.Resources), 0 ≤ Aiyagari1994.assetPolicy m z ∧ Aiyagari1994.assetPolicy m z ≤ z) ∧
          (∀ (z : Aiyagari1994.Resources),
              ↑(Aiyagari1994.consumptionPolicy m z) = ↑z - ↑(Aiyagari1994.assetPolicy m z) ∧
                Aiyagari1994.consumptionPolicy m z + Aiyagari1994.assetPolicy m z = z) ∧
            Continuous (Aiyagari1994.consumptionPolicy m)
'Aiyagari1994.assetPolicy_unique_continuous' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.assetPolicy_existsUnique_maximizer (m : Aiyagari1994.HouseholdPrimitives) (z : Aiyagari1994.Resources) :
  ∃! a,
    a ≤ z ∧
      ∀ b ≤ z,
        Aiyagari1994.bellmanObjective m (Aiyagari1994.valueFunction m) z b ≤
          Aiyagari1994.bellmanObjective m (Aiyagari1994.valueFunction m) z a
'Aiyagari1994.assetPolicy_existsUnique_maximizer' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.assetPolicy (m : Aiyagari1994.HouseholdPrimitives) (z : Aiyagari1994.Resources) : Aiyagari1994.Resources
'Aiyagari1994.assetPolicy' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\begin{verbatim}
Aiyagari1994.consumptionPolicy (m : Aiyagari1994.HouseholdPrimitives) (z : Aiyagari1994.Resources) :
  Aiyagari1994.Resources
'Aiyagari1994.consumptionPolicy' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Source correspondence:} A94 equations (4)-(7), printed
pp.~666-667 / PDF pp.~9-10, visually inspected. A93 Appendix Proposition
2, printed pp.~37-38 / PDF pp.~38-39, is the manifest context and was
also inspected. Its differentiability and envelope claims are outside
M02A; the BASIC Bellman construction is explicitly reconstructed under
architecture section 3.

\textbf{Qualification.} The asset action is shifted next-period assets,
not a share or net assets. These results do not claim policy
monotonicity, strict positivity of consumption, or H05 lifetime
optimality. Only accepted foundational axioms occur. H05 was
UNFORMALIZED at the accepted M02A boundary; the separate M02B entry
below records its implementation and external acceptance.

\section{H05 --- Canonical Policy lifetime
optimal}\label{h05-canonical-policy-lifetime-optimal}

\textbf{Status:} GREEN, externally accepted on 2026-09-11.
\textbf{Scope:} core. \textbf{Milestone:} M02B.

\textbf{Target declaration:}
\texttt{Aiyagari1994.canonicalPolicy\_lifetime\_optimal}.

\textbf{Module:} \texttt{Aiyagari1994/Household/Verification.lean}.

\textbf{Support module:} \texttt{Aiyagari1994/Household/History.lean}.

\textbf{Mathematical statement.} For every primitive household and
initial resource \(z_0\geq0\), every measurable nonanticipative feasible
plan has an absolutely convergent expected discounted utility series.
Its sum is at most \(V(z_0)\). The recursively constructed canonical
plan attains \(V(z_0)\) and hence weakly dominates every such plan.

\textbf{Actual assumptions and scope.} BASIC is the existing
\texttt{HouseholdPrimitives}: \(0<\beta<1\), utility continuous,
bounded, strictly increasing and strictly concave on nonnegative
consumption, the existing compact labor support and probability law, and
admissible normalized prices. H02 and H04 supply the constructed bounded
continuous value and continuous feasible optimal asset policy. IID is
discharged by the existing finite product \texttt{historyLaw}; there is
no new independence field. No CoreRegularity, smoothness, Inada,
\(\beta R<1\), nondegeneracy, mean-one labor, bounded assets or
stationary law is assumed. The entire NNReal resource space and a
general compactly supported labor distribution remain covered.
Dependencies P01, H02 and H04 are GREEN. Primitive consistency remains
witnessed by P03.

\textbf{History and comparison class.} \texttt{History\ m\ t} is
\texttt{Fin\ t} to labor. Coordinate zero is the newest shock.
\texttt{historySplit} is the installed measurable equivalence
\texttt{piFinSuccAbove} at zero; its two projections define
\texttt{newestShock} and \texttt{previousHistory}, and its inverse
defines \texttt{extendHistory}. Thus at date \(t\) a plan can use all
\(t\) observed shocks, with no dependence on future shocks. The empty
date-zero history is unique.

A \texttt{FeasiblePlan\ m\ z0} contains exactly an action family, its
measurability proof and pointwise feasibility. The family has type
\texttt{(t\ :\ Nat)\ -\textgreater{}\ History\ m\ t\ -\textgreater{}\ Resources}.
It may vary with time and every observed coordinate; it need not factor
through current resources and has no stationarity or optimality field.
These are measurable functions of the given shock histories. No separate
private randomization variable is added to the approved information
space; the comparison class is not restricted to deterministic time
sequences or current-state feedback. Pointwise feasibility at every
history is the explicit M02B interface, replacing the older design's
almost-everywhere wording.

\textbf{Derived resources and consumption.} The initial state is
\(z_0\). At date \(t+1\), resources are \texttt{nextResources} of the
date-\(t\) action on the previous history and the newest shock. In
particular the transition does not use the date-\(t+1\) choice. These
states are derived from the action family, not stored independently.
Measurability follows by composition of measurable history projections,
measurable actions and the continuous resource transition. Consumption
is NNReal subtraction. Feasibility gives the exact real subtraction
identity and \(c_t+a_t=z_t\), so utility always receives a nonnegative
consumption argument. Measurability of consumption and flow utility
follows by subtraction and continuity of utility on NNReal.

\textbf{Integrability and the criterion.} \texttt{utilityBound} selects
a finite bound from the proved primitive boundedness property;
\texttt{utilityBound\_spec} and \texttt{utilityBound\_nonneg} certify
it. Uniformly over plans, dates and histories, \(|U(c_t)|\leq C\). Since
each finite history law is a probability measure,
\texttt{flowUtility\_integrable} proves integrability and
\texttt{expectedFlow\_bound} proves \(|E U(c_t)|\leq C\). The terminal
value composed with measurable resources is likewise integrable, with
expected absolute value bounded by \(\|V\|_\infty\). The definition
\texttt{lifetimeUtility} is the tsum of \(\beta^t E U(c_t)\). Comparison
with \(C\beta^t\) proves absolute summability and convergence of the
finite sums to this tsum, for every feasible plan. No unproved limiting
expectation defines utility.

\textbf{The stochastic bridge.} \texttt{history\_integral\_step} takes
an explicit integrability premise on the next-history function. The
already constructed \texttt{history\_step} proves that the history split
preserves measure from the next finite product to labor law times
previous-history law. Its inverse changes variables in the integral and
transfers integrability. Mathlib's \texttt{integral\_prod\_symm} then
yields previous-history outer integration and newest-shock inner
integration. Applying this result to the next value function identifies
the integrated continuation with the next expected terminal value. IID
is proved through this product construction, rather than postulated as
an expectation identity.

\textbf{One-step and finite-horizon verification.} At every history,
feasibility places the plan's actual shifted-asset action in
\([0,z_t]\). H04 maximization and the Bellman equality give
\[U(c_t)+\beta E_l V(Ra_t+e(l))\leq V(z_t).\] All three terms have
explicit integrability theorems. Integrating this inequality and using
the history bridge gives \[E U(c_t)+\beta T_{t+1}\leq T_t,\] where
\(T_t\) is the plan's expected terminal value. At horizon zero the
history law integrates the constant initial value to \(V(z_0)\).
Induction, multiplying the one-step inequality by nonnegative
\(\beta^N\), and the finite-sum successor identity give
\[\sum_{t<N}\beta^t E U(c_t)+\beta^N T_N\leq V(z_0).\] This is the
proved \texttt{finiteHorizon\_verification}, with no telescoping
assumption.

\textbf{Canonical construction and equality.}
\texttt{canonicalResources} is defined recursively from the accepted
asset policy and resource transition. Continuity of the asset policy and
measurability of the history operations prove measurable canonical
actions. The resource family derived from these actions agrees with the
recursive family. The accepted bound \(A(z)\leq z\) certifies
feasibility, giving \texttt{canonicalPlan}. Every canonical action
attains the Bellman maximum. Integrating the pointwise equality and
repeating the same induction proves
\texttt{canonical\_finiteHorizon\_verification} as an equality for every
horizon.

\textbf{Infinite-horizon limit.} For every comparison plan,
\[|\beta^N T_N|\leq\beta^N\|V\|_\infty\longrightarrow0.\] The proof uses
only bounded \(V\) and \(0<\beta<1\), regardless of the growth of
resources or assets. The finite discounted sums converge by absolute
summability. Closedness of the real order gives lifetime utility at most
\(V(z_0)\); uniqueness of limits applied to the canonical finite-horizon
identity gives canonical lifetime utility exactly \(V(z_0)\).
Substitution proves dominance.

\textbf{Original-budget bridge.}
\texttt{original\_shifted\_trajectory\_budget\_iff} applies P01 at every
date of arbitrary real asset and consumption trajectories. Writing
shifted holdings as \(\widehat a_t=a_t+\phi\), the conjunction of the
original budget and \(a_{t+1}\geq-\phi\) is equivalent to the shifted
budget and \(\widehat a_{t+1}\geq0\). For a concrete feasible history
plan whose prices are \texttt{OriginalPrices.normalized},
\texttt{plan\_original\_budget\_step} proves the original budget on
every next history, identifying net assets with shifted actions minus
\(\phi\). The separate initial-date theorem uses the exact initial
coordinate relation. Consumption is unchanged by the coordinate shift.
These are conditional coordinate identities, not new assumptions on
H05's general normalized theorem.

\textbf{Source correspondence.} A93 Appendix Proposition 2, printed
pp.~37--38 / PDF pp.~38--39; A94 equations (5)--(7), printed
pp.~666--667 / PDF pp.~9--10. These approved source pages were visually
checked in the household source review. The finite-history product
bridge, integrability and explicit verification details are
reconstructed proofs, not claims that the papers contain Lean-ready
statements. P01 supplies the original coordinate algebra.

\textbf{Exact elaborated main theorem and transitive axioms.}

\begin{verbatim}
Aiyagari1994.canonicalPolicy_lifetime_optimal (m : Aiyagari1994.HouseholdPrimitives) (z0 : Aiyagari1994.Resources) :
  (∀ (p : Aiyagari1994.FeasiblePlan m z0), Summable fun (t : ℕ) => |m.beta ^ t * Aiyagari1994.expectedFlow p t|) ∧
    (∀ (p : Aiyagari1994.FeasiblePlan m z0), Aiyagari1994.lifetimeUtility p ≤ (Aiyagari1994.valueFunction m) z0) ∧
      Aiyagari1994.lifetimeUtility (Aiyagari1994.canonicalPlan m z0) = (Aiyagari1994.valueFunction m) z0 ∧
        ∀ (p : Aiyagari1994.FeasiblePlan m z0),
          Aiyagari1994.lifetimeUtility p ≤ Aiyagari1994.lifetimeUtility (Aiyagari1994.canonicalPlan m z0)
'Aiyagari1994.canonicalPolicy_lifetime_optimal' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Audit and review evidence.} Fresh builds of both M02B modules,
the full project build, direct \texttt{Audit.lean}, exact signature
probe, contract checker and prohibited-pattern scan pass. All 65 new
exported declarations, including the feasible-plan projections, pass
\texttt{assert\_no\_sorry} and transitive axiom printing. Across the
entire audit there are 254 checked declarations; only \texttt{propext},
\texttt{Classical.choice} and \texttt{Quot.sound} occur. Exact required
interfaces and structure fields appear in
\texttt{reports/02b\_signatures.md}; raw evidence is in
\texttt{reports/logs/02b/}. H05 is GREEN by external review in
\texttt{reviews/02b\_acceptance.md}. Lifetime utility is a series of
expectations under consistent finite-history laws; no literal
infinite-product path random variable has been constructed. At the
accepted M02B boundary, H06 and all later contracts were UNFORMALIZED;
no M03 work was included. The M03A entries below record the separate
H07/H08 review submission.

\section{H06 --- Policy jointly
continuous}\label{h06-policy-jointly-continuous}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
04.

\textbf{Target declaration:}
\texttt{Aiyagari1994.policy\_jointly\_continuous}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Household/ParameterContinuity.lean}.

\textbf{Mathematical contract.} With U, nu and beta fixed, V\_theta(z)
and A\_theta(z) are jointly continuous in state and admissible
normalized prices, including beta*R=1 and beta*R\textgreater1.

\textbf{Assumption profiles:} BASIC. These are branch-sensitive context
tags; the completed signature must list the actual premises.

\textbf{Dependencies:} H02, H04. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 2, printed pp.~37-38 /
PDF pp.~38-39; A94 equations (5)-(7), printed pp.~666-667 / PDF
pp.~9-10. Lifetime and parameter details are reconstructed explicitly.

\textbf{Readable proof plan:} Architecture §6.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{H07 --- Policy order and Lipschitz
bounds}\label{h07-policy-order-and-lipschitz-bounds}

\textbf{Status:} GREEN, externally accepted on 2026-09-11; see
\texttt{reviews/03a\_acceptance.md}. \textbf{Scope:} core.
\textbf{Review gate:} M03A.

\textbf{Target:} \texttt{Aiyagari1994.policies\_order\_lipschitz}.

\textbf{Module:} \texttt{Aiyagari1994/Household/PolicyOrder.lean}.

\textbf{Mathematical statement.} Both shifted saving \(A\) and
consumption \(c\) are nondecreasing. For \(0\leq z_1\leq z_2\),
\[0\leq A(z_2)-A(z_1)\leq z_2-z_1,\qquad 0\leq c(z_2)-c(z_1)\leq z_2-z_1.\]
The public theorem expresses these differences in Real with explicit
NNReal coercions. It makes no strict-order or policy-derivative claim.

\textbf{Actual assumptions and dependencies.} BASIC only. H03 supplies
concavity of the constructed value, H04 supplies the unique maximizing
shifted-asset policy and the consumption budget. The proof uses no H05
lifetime-optimality result. No impatience, smoothness, Inada, income
nondegeneracy, mean-one labor, stationary law, asset bound or
CoreRegularity enters. The state remains all NNReal and labor retains
its general compactly supported probability law.

\textbf{Reusable concavity inequality.} \texttt{concave\_four\_point}
proves that, for \(f\) concave on \([0,\infty)\),
\[0\leq x<y,\quad x\leq u,v\leq y,\quad u+v=x+y
\quad\Longrightarrow\quad f(x)+f(y)\leq f(u)+f(v).\] For \(w=u,v\),
write \[w=\frac{y-w}{y-x}x+\frac{w-x}{y-x}y.\] Concavity gives the two
weighted inequalities. Adding them and using \(u+v=x+y\) makes each
endpoint coefficient equal one. This helper is proved directly from the
definition of concavity, without a comparative-statics assumption.

\textbf{Savings order.} Suppose \(A(z_2)<A(z_1)\). Both actions are
feasible at both states. The original consumptions are the endpoints
\(z_1-A(z_1)\) and \(z_2-A(z_2)\). Swapping savings gives the two inward
consumptions, preserving their sum. Apply the four-point inequality to
utility. The sum of the two Bellman optimality inequalities has
identical continuation terms on either side; together with the concavity
inequality it forces equality in the lower-state comparison. The
reversed action is therefore also optimal at \(z_1\). H04's unique
maximizer makes the two actions equal, contradicting reversal. Strict
concavity of utility enters through this already proved uniqueness
theorem; the new comparison uses only weak concavity.

\textbf{Consumption order.} This is a separate argument. Suppose
\(c(z_2)<c(z_1)\). Compare the canonical savings with \(b_1=z_1-c(z_2)\)
and \(b_2=z_2-c(z_1)\). Both are nonnegative and feasible at their
respective states. The two new savings lie between the original savings
and preserve their sum. The continuation function is concave by H03, the
affine transition and integration. Apply the four-point inequality to
continuation, multiply by nonnegative beta, and combine the two
optimality inequalities. Current utilities cancel because the comparison
swaps consumption. Equality again gives a second lower-state optimizer,
contradicting H04 uniqueness and the assumed consumption reversal.

\textbf{Lipschitz bounds.} The exact budget is \(z=A(z)+c(z)\).
Nonnegativity of each difference follows from its own monotonicity.
Nonnegativity of the other component's difference supplies its upper
bound by \(z_2-z_1\). No differentiability or strict monotonicity is
inferred.

\textbf{Source correspondence.} Approved locator: A93 Appendix
Proposition 2, printed pp.~37--38 / PDF pp.~38--39; A94 equations
(5)--(7), printed pp.~666--667 / PDF pp.~9--10. The A93 pages were
visually checked again for M03A. The weak-order proof is reconstructed
explicitly from architecture section 4; stronger envelope, Euler and
strict-order claims remain outside this gate.

\textbf{Exact elaborated public theorem and axiom output.}

\begin{verbatim}
Aiyagari1994.policies_order_lipschitz (m : Aiyagari1994.HouseholdPrimitives) :
  Monotone (Aiyagari1994.assetPolicy m) ∧
    Monotone (Aiyagari1994.consumptionPolicy m) ∧
      ∀ (z1 z2 : Aiyagari1994.Resources),
        z1 ≤ z2 →
          (0 ≤ ↑(Aiyagari1994.assetPolicy m z2) - ↑(Aiyagari1994.assetPolicy m z1) ∧
              ↑(Aiyagari1994.assetPolicy m z2) - ↑(Aiyagari1994.assetPolicy m z1) ≤ ↑z2 - ↑z1) ∧
            0 ≤ ↑(Aiyagari1994.consumptionPolicy m z2) - ↑(Aiyagari1994.consumptionPolicy m z1) ∧
              ↑(Aiyagari1994.consumptionPolicy m z2) - ↑(Aiyagari1994.consumptionPolicy m z1) ≤ ↑z2 - ↑z1
'Aiyagari1994.policies_order_lipschitz' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Verification:} fresh module build, full build, direct audit and
no-sorry/axiom checks pass. Only standard foundational axioms occur.
External review accepted H07; see \texttt{reviews/03a\_acceptance.md}.

\section{H08 --- Right marginal value}\label{h08-right-marginal-value}

\textbf{Status:} GREEN, externally accepted on 2026-09-11; see
\texttt{reviews/03a\_acceptance.md}. \textbf{Scope:} core.
\textbf{Review gate:} M03A.

\textbf{Target:} \texttt{Aiyagari1994.rightMarginalValue\_properties}.

\textbf{Module:} \texttt{Aiyagari1994/Household/RightMarginal.lean}.

\textbf{Mathematical statement.} At every positive real state the
canonical right marginal exists as a finite Real, is strictly positive,
nonincreasing and continuous from the right, and satisfies
\[0<q(z)\leq\frac{V(z)-V(0)}z\leq\frac{\operatorname{osc}(U)}{(1-\beta)z}.\]
At zero a separate ENNReal marginal preserves the possible infinite
limit. Positive-state marginals converge increasingly to this extended
value as states decrease to zero.

\textbf{Actual assumptions and dependencies.} BASIC only, through the
accepted H03 concavity and strict increase. H02's exact value bounds
supply the oscillation estimate. This bound uses existing constructed
objects, not a new hypothesis; the manifest dependency tags remain
unchanged. The module imports value analysis, not PolicyOrder or
lifetime verification. No derivative of utility, value or policy is
assumed. No CoreRegularity, impatience, Inada, stationary law or
bounded-state assumption enters.

\textbf{Real-domain representation.} \texttt{valueExtension\ m\ x} is
\(V(x.\mathrm{toNNReal})\). \texttt{NNRealConcave.extension} converts
the accepted NNReal convex-combination inequality to real concavity on
\([0,\infty)\). The extension is continuous and strictly increasing on
that economic domain. Concavity is never asserted for negative
arguments.

\textbf{Secants and canonical construction.} Write
\[S(z,y)=\frac{V(y)-V(z)}{y-z},\qquad y>z\geq0.\] The increment form is
exactly \(S(z,z+h)=[V(z+h)-V(z)]/h\), certified by
\texttt{rightSecant\_increment}. Concavity yields
\[z<y_1\leq y_2\quad\Longrightarrow\quad S(z,y_2)\leq S(z,y_1).\] Apply
the pinned Mathlib theorem for a convex function's right secant infimum
to \(-V\) on \([0,\infty)\) at the interior point \(z>0\). That theorem
proves boundedness of the secants using a point to the left and
constructs their finite one-sided limit. Define
\[q(z)=-\inf\{S_{-V}(z,y):y\geq0,\ y>z\}.\] Negating the proved limit
gives \texttt{rightMarginalValue\_hasDerivWithinAt} and the explicit
secant limit. There is no ordinary \texttt{deriv} definition. A separate
bridge proves the equivalent, finite supremum representation
\[q(z)=\sup_{y>z}S(z,y).\] The real function's economic interpretation
is restricted to positive states; its value at zero is not the boundary
marginal.

\textbf{Secant inequalities and positivity.} Every right secant obeys
\(S(z,y)\leq q(z)\). For \(0\leq x<z<t\), concavity gives
\(S(z,t)\leq S(x,z)\). Letting \(t\) decrease to \(z\) gives
\(q(z)\leq S(x,z)\). Consequently,
\[q(y)\leq S(z,y)\leq q(z)\qquad(0<z<y).\] This proves monotonicity.
Strict increase of \(V\) makes \(S(z,z+1)>0\); hence \(q(z)>0\) without
differentiability assumptions. Choosing \(x=0\) gives the left-secant
bound.

\textbf{Oscillation bound.} \texttt{utilityOscillation} is precisely
\texttt{utilitySup\ -\ utilityInf}, using H02's bounded utility range.
Subtract the lower bound for \(V(0)\) from the upper bound for \(V(z)\):
\[V(z)-V(0)\leq\frac{U_{\rm sup}-U_{\rm inf}}{1-\beta}.\] Divide by
positive \(z\). This yields the stated estimate on all positive states,
with no artificial cutoff.

\textbf{Right continuity, proved.} For fixed \(z>0\), monotonicity
bounds \(q(x)\) for \(x>z\) above by \(q(z)\). Let \(L=\sup_{x>z}q(x)\).
The monotone-limit theorem gives \(q(x)\to L\) as \(x\downarrow z\) and
\(L\leq q(z)\). For fixed \(y>z\) and nearby \(z<x<y\),
\[S(x,y)\leq q(x)\leq L.\] Continuity of \(V\) and the nonzero fixed
secant denominator give \(S(z,y)\leq L\) on sending \(x\downarrow z\).
Send \(y\downarrow z\) using the already proved secant limit to get
\(q(z)\leq L\). Thus \(L=q(z)\). The public result is
\texttt{ContinuousWithinAt\ (rightMarginalValue\ m)\ (Ici\ z)\ z} for
every \(z>0\), not merely semicontinuity; it implies convergence along
every positive sequence decreasing to \(z\).

\textbf{Extended boundary marginal.} Define
\[q_0=\sup_{y>0}\operatorname{ofReal}(S(0,y))\quad\hbox{in ENNReal}.\]
Every secant here is positive, so the nonnegative conversion does not
discard an economic sign. The antitone secants converge to this
complete-order supremum as \(y\downarrow0\). The inequality
\(q(z)\leq S(0,z)\) bounds every converted positive marginal by \(q_0\).
Conversely, fix \(y>0\). For \(0<x<y\), \(S(x,y)\leq q(x)\); continuity
at zero allows \(x\downarrow0\), and then taking the supremum over \(y\)
proves \[q_0=\sup_{z>0}\operatorname{ofReal}(q(z)).\] Since positive
marginals are antitone in state, the complete-order monotone-limit
theorem now proves their convergence to \(q_0\) as \(z\downarrow0\),
even when the limit is infinity. The theorem
\texttt{zeroRightMarginal\_eq\_top\_iff} characterizes infinity by
arbitrarily large converted positive marginals. No theorem assumes or
concludes universal finiteness at zero. This gate does not construct a
separate economic example with infinite boundary marginal and does not
impose an Inada condition.

\textbf{Source correspondence.} This is new
concave-analysis/value-marginal infrastructure under architecture
section 4.1, inspired by the CW00 context in the manifest, not a literal
numbered Aiyagari theorem or an imported economic result. The actual
imported analysis is from the pinned Mathlib source: convex
negative-value right secant limits, concave secant order and monotone
limits. A93's broader differentiability/envelope statements are not
inferred here.

\textbf{Exact elaborated public theorem and axiom output.}

\begin{verbatim}
Aiyagari1994.rightMarginalValue_properties (m : Aiyagari1994.HouseholdPrimitives) :
  (∀ (z : ℝ),
      0 < z →
        Filter.Tendsto (slope (Aiyagari1994.valueExtension m) z) (nhdsWithin z (Set.Ioi z))
            (nhds (Aiyagari1994.rightMarginalValue m z)) ∧
          0 < Aiyagari1994.rightMarginalValue m z ∧
            ContinuousWithinAt (Aiyagari1994.rightMarginalValue m) (Set.Ici z) z ∧
              Aiyagari1994.rightMarginalValue m z ≤
                  (Aiyagari1994.valueExtension m z - Aiyagari1994.valueExtension m 0) / z ∧
                (Aiyagari1994.valueExtension m z - Aiyagari1994.valueExtension m 0) / z ≤
                  Aiyagari1994.utilityOscillation m / ((1 - m.beta) * z)) ∧
    AntitoneOn (Aiyagari1994.rightMarginalValue m) (Set.Ioi 0) ∧
      Filter.Tendsto (fun (y : ℝ) => ENNReal.ofReal (slope (Aiyagari1994.valueExtension m) 0 y))
          (nhdsWithin 0 (Set.Ioi 0)) (nhds (Aiyagari1994.zeroRightMarginal m)) ∧
        Filter.Tendsto (fun (z : ℝ) => ENNReal.ofReal (Aiyagari1994.rightMarginalValue m z)) (nhdsWithin 0 (Set.Ioi 0))
          (nhds (Aiyagari1994.zeroRightMarginal m))
'Aiyagari1994.rightMarginalValue_properties' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Exact canonical definition types.}

\begin{verbatim}
Aiyagari1994.rightMarginalValue (m : Aiyagari1994.HouseholdPrimitives) (z : ℝ) : ℝ
'Aiyagari1994.rightMarginalValue' depends on axioms: [propext, Classical.choice, Quot.sound]
Aiyagari1994.zeroRightMarginal (m : Aiyagari1994.HouseholdPrimitives) : ENNReal
'Aiyagari1994.zeroRightMarginal' depends on axioms: [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Verification and boundary of the predecessor gate.} Both M03A
targets and all supporting exports pass fresh builds, the full build,
direct \texttt{Audit.lean}, no-sorry assertions, the prohibited-pattern
scan and transitive axiom checks. Only \texttt{propext},
\texttt{Classical.choice} and \texttt{Quot.sound} occur. The exact
signature report contains all secant, right-continuity and
boundary-limit interfaces plus the printed definitions. H07 and H08 are
GREEN by external review; H01--H05 remain GREEN. Historically, at M03A
acceptance, H09 and later targets were UNFORMALIZED. Current statuses
are generated in the economic-status overview and individual contract
headings. H09 passed independent Astra review; see
\texttt{reviews/m03b1\_acceptance.md} and its structured counterpart.
The H10 heading records its current independent-review status. The
accepted boundary qualification remains mandatory: never use
\texttt{rightMarginalValue\ m\ 0} as the economic zero-state marginal;
use the separate extended \texttt{zeroRightMarginal}.

\section{H09 --- Right Marginal Value
superharmonic}\label{h09-right-marginal-value-superharmonic}

\textbf{Status:} GREEN. Independent Astra acceptance:
\texttt{reviews/m03b1\_acceptance.md}.

\textbf{Scope:} core. \textbf{Milestone:} 03. \textbf{Review gate:}
M03B1.

\textbf{Target declaration:}
\texttt{Aiyagari1994.rightMarginalValue\_superharmonic}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Household/MarginalInequality.lean}.

\textbf{Mathematical contract.} For every z with finite right marginal,
q(z)\textgreater=beta*R*Integral q(R*A(z)+e) dnu. Establish the
extended-integral inequality first and deduce conditional integrability;
it is valid without beta*R\textless1 and without consumption positivity.

\textbf{Assumption profiles:} BASIC. These are branch-sensitive context
tags; the completed signature must list the actual premises.

\textbf{Dependencies:} H04, H08. \textbf{Source keys:} CW00.

\textbf{Source locator:} New concave-analysis/value-marginal
infrastructure for the Aiyagari claims; inspired by CW00 Sections 2-4.
Not a literal numbered Aiyagari theorem.

\textbf{Exact state-space interface.} The new ENNReal-valued marginal is
defined on all \texttt{Resources}. At \texttt{z=0} it is exactly
\texttt{zeroRightMarginal\ m}; at positive \texttt{z} it converts the
accepted finite real marginal with \texttt{ENNReal.ofReal}. Thus the
economic zero marginal can be infinite, and the real marginal function
is never evaluated economically at zero.

The primary extended theorem proves the unconditional \texttt{lintegral}
inequality. The contracted theorem
\texttt{rightMarginalValue\_superharmonic} assumes only that the left
marginal is finite and exposes almost-everywhere continuation
finiteness, Bochner integrability of \texttt{toReal}, and the real
integral inequality.

\textbf{Actual assumptions and dependencies.} BASIC only. The proof uses
H04's canonical Bellman optimizer and H08's concave right-secant limits,
including its separate extended boundary limit. Positivity of beta and
gross return comes from \texttt{HouseholdPrimitives}. There is no
\texttt{beta*R\textless{}1}, consumption-positivity, smoothness, Inada,
nondegeneracy, stationarity, invariant law, or bounded-asset premise.
The state is all \texttt{NNReal}, and the labor law is the original
general compactly supported probability law.

\textbf{Readable proof.} Fix a state \texttt{z}, its canonical shifted
saving \texttt{A(z)}, and \texttt{h\textgreater{}0}. At state
\texttt{z+h}, choose shifted saving \texttt{A(z)+h}. It is feasible
because \texttt{A(z)\textless{}=z}, and it preserves current consumption
exactly. Bellman optimality therefore gives \[
 V(z+h)-V(z)\geq\beta\int
 [V(RA(z)+e(\ell)+Rh)-V(RA(z)+e(\ell))],d\nu(\ell).
\] The finite-h integrands are continuous and integrable. Divide by
\texttt{h}; rewriting each continuation quotient as a secant introduces
the exact positive factor \texttt{R}: \[
 S(z,z+h)\geq\beta R\int S(x(\ell),x(\ell)+Rh),d\nu(\ell),
 \qquad x(\ell)=RA(z)+e(\ell).
\] Every secant is nonnegative by strict increase of value, so the
proved finite-h real integral identity can be converted to a
\texttt{lintegral} without assigning economic meaning to an unproved
marginal integral.

Take \texttt{h\_n=1/(n+1)}. Concavity makes the nonnegative continuation
secants increase pointwise as \texttt{n} increases. At a positive
continuation state H08 identifies their limit with \texttt{ofReal(q)};
at a zero continuation state it identifies the limit with the separate
\texttt{zeroRightMarginal}. Mathlib's monotone convergence theorem for
\texttt{lintegral} therefore gives \[
 \operatorname{ofReal}(\beta R)\int^- \bar q(RA(z)+e(\ell)),d\nu(\ell)
 \leq \bar q(z),
\] where \texttt{bar\ q} denotes \texttt{extendedRightMarginalValue}.
This theorem holds even if either side is infinite.

If \texttt{bar\ q(z)\textless{}infinity}, positivity of beta and
\texttt{R} makes the extended continuation integral finite.
Measurability plus this finite \texttt{lintegral} proves
\texttt{bar\ q(x(ell))\textless{}infinity} almost everywhere and
integrability of its \texttt{toReal}. Only then does
\texttt{integral\_toReal} identify the real integral with the extended
integral's finite real value and yield \[
 \beta R\int \bar q(RA(z)+e(\ell)).\mathrm{toReal}\,d\nu(\ell)
 \leq \bar q(z).\mathrm{toReal}.
\] At positive \texttt{z}, H08 makes the right side exactly the finite
real \texttt{q(z)}. At zero, the premise and conclusion continue to
refer to \texttt{zeroRightMarginal}.

\textbf{Source correspondence.} CW00 motivates the value-marginal
supermartingale inequality in its overview, printed p.~367 / PDF p.~3,
and states the corresponding conditional right-value- derivative
inequality in Lemma 1(a), printed p.~372 / PDF p.~8. The paper calls the
proof standard and omits it; the Lean proof is the new
secant/monotone-convergence construction required by architecture
section 4.1, not a formalization of a literal numbered Aiyagari theorem.

\textbf{Exact elaborated contract signature.} The full kernel print,
including the extended prerequisite theorem, is also in the M03B1
signature report and probe.

\begin{verbatim}
Aiyagari1994.rightMarginalValue_superharmonic
  (m : Aiyagari1994.HouseholdPrimitives)
  (z : Aiyagari1994.Resources)
  (hz : Aiyagari1994.extendedRightMarginalValue m z < ⊤) :
  Filter.Eventually
    (fun l : m.income.Labor =>
      Aiyagari1994.extendedRightMarginalValue m
        (m.prices.nextResources (Aiyagari1994.assetPolicy m z) l) < ⊤)
    (Measure.ae (m.income.law : Measure m.income.Labor)) ∧
  MeasureTheory.Integrable
    (fun l : m.income.Labor =>
      (Aiyagari1994.extendedRightMarginalValue m
        (m.prices.nextResources (Aiyagari1994.assetPolicy m z) l)).toReal)
    (m.income.law : Measure m.income.Labor) ∧
  m.beta * m.prices.grossReturn * ∫ l : m.income.Labor,
      (Aiyagari1994.extendedRightMarginalValue m
        (m.prices.nextResources (Aiyagari1994.assetPolicy m z) l)).toReal
      ∂(m.income.law : Measure m.income.Labor) ≤
    (Aiyagari1994.extendedRightMarginalValue m z).toReal
'Aiyagari1994.rightMarginalValue_superharmonic' depends on axioms:
  [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Audit result.} The contracted declaration and every new public
helper are checked by \texttt{Audit.lean} with \texttt{\#check},
\texttt{assert\_no\_sorry}, and \texttt{\#print\ axioms}. The transitive
axiom output contains only \texttt{propext}, \texttt{Classical.choice},
and \texttt{Quot.sound}. Kernel checking is complete. H09 passed
independent Astra review and is GREEN; see
\texttt{reviews/m03b1\_acceptance.md} and
\texttt{reviews/m03b1\_acceptance.json}. All qualifications in that
acceptance, including the finite-left boundary qualification, remain in
force.

\section{H10 --- Consumption positive
subcritical}\label{h10-consumption-positive-subcritical}

\textbf{Status:} GREEN. Independent Astra acceptance:
\texttt{reviews/m03b2\_acceptance.md}.

\textbf{Scope:} core. \textbf{Milestone:} 03. \textbf{Review gate:}
M03B2.

\begin{itemize}
\tightlist
\item
  \textbf{Target declaration:}
  \texttt{Aiyagari1994.consumption\_positive\_subcritical}.
\item
  \textbf{Module:}
  \texttt{Aiyagari1994/Household/ConsumptionPositive.lean}.
\end{itemize}

\textbf{Mathematical contract.} If beta*R\textless1, then
c(z)\textgreater0 at every z\textgreater0, allowing either a finite or
infinite right marginal of U at zero.

\textbf{Assumption profiles:} BASIC, SMOOTH, IMPATIENT. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H04, H08. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 2, printed pp.~37-38 /
PDF pp.~38-39; A94 equations (5)-(7), printed pp.~666-667 / PDF
pp.~9-10. Lifetime and parameter details are reconstructed explicitly.

\textbf{Exact state-space and boundary interface.} The theorem
quantifies over every positive \texttt{z\ :\ Resources} on the
continuous \texttt{NNReal} state space. The new object
\texttt{utilityZeroRightMarginal\ m\ :\ ENNReal} is the supremum of
nonnegative utility secants from zero and may equal infinity. This is
distinct from the accepted value boundary object
\texttt{zeroRightMarginal\ m\ :\ ENNReal}. The proof never evaluates
\texttt{rightMarginalValue\ m\ 0} and does not identify the right
marginal of value with the right marginal of utility at a corner.

\textbf{Actual and transitive economic assumptions.}

The public signature takes \texttt{m\ :\ HouseholdPrimitives},
\texttt{UtilitySmooth\ m.utility}, and the strict impatience premise
\texttt{m.beta\ *\ m.prices.grossReturn\ \textless{}\ 1}. Thus the
exposed profiles are exactly BASIC, SMOOTH, and IMPATIENT. The
mathematical proof uses BASIC's bounded continuous strictly increasing
strictly concave utility, positive beta and return, H04's constructed
canonical optimizer, H08's finite positive-state right value marginal,
and IMPATIENT. Smoothness is retained in the contracted interface for
the source theorem family, although the secant proof establishes this
positivity conclusion without differentiating utility. No curvature,
nondegeneracy, income density or atom, positive minimum effective
income, stationarity, asset bound, or integrability of a marginal
expectation is assumed. H09 is accepted but is not needed by this proof;
the manifest dependencies remain exactly H04 and H08.

\textbf{Readable proof.} Define the zero utility marginal by \[
 L_0=\sup_{h>0}\operatorname{ofReal}
       \frac{U(h)-U(0)}h\in[0,\infty].
\] Strict increase makes \(L_0>0\), while concavity makes the secants
antitone in \(h\) and gives their extended-real limit \(L_0\) as
\(h\downarrow0\).

First suppose \(L_0<\infty\) and write \(L=L_0.\mathrm{toReal}\).
Concavity bounds every economic-domain utility increment by \(L(y-x)\).
Starting from the zero function, finite-horizon induction proves that
the Bellman operator preserves the same ordered Lipschitz bound whenever
\(\beta R\leq1\). If the optimizer at the larger state is feasible at
the smaller state, keep that action fixed. Otherwise use the smaller
state's consume-zero action: the resource increment splits between
current consumption and additional saving, whose discounted continuation
contribution is bounded by \(\beta R L\) and hence by \(L\). Uniform
convergence of Bellman iterates passes the bound to the canonical value
function and integration passes it to the continuation value.

If \(c(z)=0\), then H04's budget identity gives \(A(z)=z\). For
\(0<h\leq z\), choosing \(z-h\) instead yields current consumption
\(h\). Optimality and the continuation Lipschitz bound imply \[
 \frac{U(h)-U(0)}h\leq\beta R L.
\] Taking the extended secant limit gives \(L\leq\beta R L\),
contradicting \(L>0\) and \(\beta R<1\).

If \(L_0=\infty\), retain saving \(A(z)=z\) at the larger state \(z+h\).
This consumes the entire increment without changing continuation value,
so \[
 \frac{U(h)-U(0)}h\leq\frac{V(z+h)-V(z)}h.
\] The left side tends to infinity in \texttt{ENNReal}; H08 identifies
the right side's limit with the finite positive-state
\texttt{rightMarginalValue\ m\ z}. This is impossible. This branch uses
neither a real conversion of the infinite endpoint marginal nor an
integral.

\textbf{Exact elaborated contract signature.} Full helper signatures and
kernel prints are in \texttt{reports/m03b2\_signatures.md} and
\texttt{Probes/M03B2Signatures.lean}.

\begin{verbatim}
Aiyagari1994.consumption_positive_subcritical
  (m : Aiyagari1994.HouseholdPrimitives)
  (_hsmooth : Aiyagari1994.UtilitySmooth m.utility)
  (hbetaR : m.beta * m.prices.grossReturn < 1)
  (z : Aiyagari1994.Resources) :
  0 < z → 0 < Aiyagari1994.consumptionPolicy m z
'Aiyagari1994.consumption_positive_subcritical' depends on axioms:
  [propext, Classical.choice, Quot.sound]
\end{verbatim}

\textbf{Source correspondence.} A93 Appendix Proposition 2(a), printed
p.~37 through the first line of printed p.~38 / PDF pp.~38-39, states
positive consumption and gives the source's concavity case argument. A94
equations (5)-(7), printed pp.~666-667 / PDF pp.~9-10, give the Bellman
problem, shifted saving policy and resource transition. The
finite/infinite endpoint split and finite-horizon Lipschitz construction
are the explicit reconstruction required by architecture section 4.2; no
lifetime or later envelope/Euler conclusion is claimed here.

\textbf{Audit result.} The contracted theorem and every new public
helper are checked in \texttt{Audit.lean} by \texttt{\#check},
\texttt{assert\_no\_sorry}, and \texttt{\#print\ axioms}. The only
transitive Lean axioms are \texttt{propext}, \texttt{Classical.choice},
and \texttt{Quot.sound}, inherited from the canonical Bellman
construction. Kernel checking supports REVIEW\_READY only; no economic
adequacy or GREEN status is self-awarded.

\section{H11 --- Value envelope at positive
consumption}\label{h11-value-envelope-at-positive-consumption}

\textbf{Status:} GREEN. Independent Astra acceptance:
\texttt{reviews/m03b3\_acceptance.md}.

\textbf{Scope:} core. \textbf{Milestone:} M03B3.

\textbf{Target declaration:}
\texttt{Aiyagari1994.value\_envelope\_at\_positive\_consumption}.\\
\textbf{Module:} \texttt{Aiyagari1994/Household/Envelope.lean}.

\textbf{Mathematical contract.} For every canonical household model,
positive resource state \(z\), and positive canonical consumption
\(c(z)\), the real extension of \(V\) has derivative \(U'(c(z))\) at
\(z\). Its previously constructed positive-state right marginal
therefore equals the same utility derivative. The theorem is local and
has no \(\beta R<1\) premise.

\textbf{Actual and transitive economic assumptions.}
\texttt{HouseholdPrimitives\ m} supplies BASIC: \(0<\beta<1\), bounded
continuous strictly increasing and strictly concave utility on
nonnegative consumption, a general compact positive labor support and
probability law, \(R>0\), \(w>0\), and nonnegative effective income.
\texttt{UtilitySmooth\ m.utility} supplies SMOOTH: \(C^1\) regularity
and a positive derivative on strictly positive consumption. The local
hypotheses are \texttt{0\ \textless{}\ z} and
\texttt{0\ \textless{}\ consumptionPolicy\ m\ z}. No IMPATIENT,
CURVATURE, atom, density, nondegeneracy, stationary-law, asset-bound,
endpoint-derivative, or consumption-positivity theorem is assumed.

\textbf{Dependencies:} H03, H04, H08. \textbf{Source keys:} BS79, A93.

\textbf{Source locator:} A93 Appendix Proposition 2, printed pp.~37-38 /
PDF pp.~38-39; BS79 Lemma 1, printed p.~728 / PDF p.~3.

\textbf{Exact elaborated signatures.} Lean reports:

\begin{verbatim}
Aiyagari1994.concave_hasDerivAt_of_lowerTouching {S : Set ℝ} {f g : ℝ → ℝ} {x d : ℝ}
  (hf : ConcaveOn ℝ S f) (hx : x ∈ interior S) (hg : HasDerivAt g d x)
  (heq : g x = f x) (htouch : ∀ᶠ (y : ℝ) in nhds x, g y ≤ f y) : HasDerivAt f d x

Aiyagari1994.value_envelope_at_positive_consumption
  (m : Aiyagari1994.HouseholdPrimitives)
  (hsmooth : Aiyagari1994.UtilitySmooth m.utility)
  (z : Aiyagari1994.Resources) (hz : 0 < z)
  (hc : 0 < Aiyagari1994.consumptionPolicy m z) :
  HasDerivAt (Aiyagari1994.valueExtension m)
      (deriv m.utility.utility ↑(Aiyagari1994.consumptionPolicy m z)) ↑z ∧
    Aiyagari1994.rightMarginalValue m ↑z =
      deriv m.utility.utility ↑(Aiyagari1994.consumptionPolicy m z)
\end{verbatim}

\textbf{Readable proof.} The generic one-dimensional lemma constructs
the finite right derivative of a concave real function at an interior
point from its right secants. Concavity orders every left secant above
that right derivative. Because the differentiable function touches from
below and agrees at the point, its right secants lie below the concave
function's right secants, while the concave function's left secants lie
below the touching function's left secants. Taking limits identifies the
right derivative with the touching derivative and squeezes the left
secants to the same value. The two one-sided secant limits give an
ordinary derivative.

For the economic wrapper, fix the actual shifted optimizer \(A(z)\) and
define \(W(x)=U(x-A(z))+\beta\,\mathrm{continuation}(A(z))\). Positive
consumption gives \(A(z)<z\), so on the open neighborhood \(x>A(z)\) the
action remains feasible and its consumption remains strictly positive.
H04 optimality gives \(W(x)\leq V(x)\) there and equality at \(z\).
SMOOTH differentiates \(W\) with derivative \(U'(c(z))\). H03 concavity
and the lower-touching lemma yield the full derivative of the value
extension. Uniqueness of the right secant limit identifies H08's
\texttt{rightMarginalValue} with that derivative.

\textbf{Boundary and integrability audit.} H11 is stated only at
positive \(z\) and positive \(c(z)\). It never evaluates
\texttt{rightMarginalValue\ m\ 0} and never uses or alters the distinct
\texttt{zeroRightMarginal\ :\ ENNReal}. The continuation term is
constant as \(x\) varies; no differentiation under an integral and no
marginal expectation occurs. Its underlying bounded value integral is
the already-integrable H01 continuation object, so no new real-integral
economic interpretation is made.

\textbf{Source correspondence.} A93 Appendix Proposition 2(c), printed
pp.~37-38 / PDF pp.~38-39, states the envelope equality and explicitly
attributes it to Benveniste--Scheinkman. BS79 Lemma 1, printed p.~728 /
PDF p.~3, gives the differentiable lower-touching criterion for a
concave value function. The Lean helper proves the required
one-dimensional version directly from left/right secants rather than
importing the paper as an axiom.

\textbf{Audit result.} Both new exported declarations are checked in
\texttt{Audit.lean} with \texttt{\#check}, \texttt{assert\_no\_sorry},
and \texttt{\#print\ axioms}. Their transitive Lean axioms are exactly
\texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound};
the economic theorem inherits choice from the canonical optimizer/fixed
point. Kernel checking supports REVIEW\_READY only. No adequacy
certification or GREEN status is asserted.

\section{H12 --- Euler subcritical}\label{h12-euler-subcritical}

\textbf{Status:} GREEN. Independent Astra acceptance:
\texttt{reviews/m03c\_acceptance.md}.

\textbf{Scope:} core. \textbf{Milestone:} M03C.

\textbf{Target declaration:} \texttt{Aiyagari1994.euler\_subcritical}.\\
\textbf{Module:} \texttt{Aiyagari1994/Household/Euler.lean}.

\textbf{Exact signature (abridged only by notation).} For
\texttt{m\ :\ HouseholdPrimitives},
\texttt{hsmooth\ :\ UtilitySmooth\ m.utility},
\texttt{hbetaR\ :\ m.beta\ *\ m.prices.grossReturn\ \textless{}\ 1},
\texttt{z\ :\ Resources}, and \texttt{hz\ :\ 0\ \textless{}\ z},
\texttt{euler\_subcritical} proves: (i) the conditional
\texttt{eulerNextMarginal} is finite almost everywhere; (ii) its real
conversion is integrable; (iii)
\texttt{beta\ *\ R\ *\ integral\ eulerNextMarginal.toReal\ \textless{}=\ deriv\ U\ (c\ z)};
and (iv), if \texttt{0\ \textless{}\ assetPolicy\ m\ z}, ordinary
next-period marginal utility is integrable, agrees pointwise with the
explicit extended marginal, and the preceding inequality is an equality.
The complete elaborated signature is in
\texttt{reports/m03c\_signatures.md}.

\texttt{eulerNextMarginal\ m\ x\ :\ ENNReal} is
\texttt{zeroRightMarginal\ m} when \texttt{x=0} and
\texttt{ENNReal.ofReal\ (deriv\ U\ (c\ x))} when
\texttt{x\textgreater{}0}. Thus the conditional expectation has an
explicit economic boundary object and never uses
\texttt{rightMarginalValue\ m\ 0} or a default real derivative at zero.

\textbf{Actual and transitive economic assumptions.} BASIC is carried by
\texttt{HouseholdPrimitives}: \(0<\beta<1\), bounded continuous strictly
increasing and strictly concave utility on nonnegative consumption, a
compact positive labor support with a probability law, \(R>0\), and
nonnegative effective income. H12 adds SMOOTH (\texttt{UtilitySmooth})
and IMPATIENT (\texttt{beta*R\textless{}1}). It also quantifies over a
positive state. Interior equality adds only
\texttt{0\ \textless{}\ assetPolicy\ m\ z}. There is no curvature,
nondegeneracy, density, atom, positive income floor, stationary law, or
bounded-asset assumption.

\textbf{Dependencies:} H09, H10, H11. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 2, printed pp.~37-38 /
PDF pp.~38-39; A94 equations (5)-(7), printed pp.~666-667 / PDF
pp.~9-10. Lifetime and parameter details are reconstructed explicitly.

\textbf{Readable proof.} H10 makes current consumption positive, and H11
identifies the finite positive-state value marginal with current
marginal utility. H09 first supplies its unconditional \texttt{ENNReal}
superharmonic inequality; finiteness of the positive current marginal
then proves the conditional \texttt{lintegral} finite and only afterward
yields a real integrable conditional marginal. The pointwise bridge
\texttt{eulerNextMarginal\_eq\_extendedRightMarginalValue} rewrites this
certified H09 expectation into H12's utility marginal at positive next
states while retaining \texttt{zeroRightMarginal} at a zero next state.

For positive shifted savings, every next resource is at least
\(RA(z)>0\). The assigned positive-asset continuation helper works on
the common neighborhood \(a>A(z)/2\): concavity bounds every absolute
continuation derivative by the constant \(R q(RA(z)/2)\), which is
integrable under the probability law. Differentiation under the integral
therefore proves both conditional marginal-utility integrability and the
derivative \(R\mathbb E U'(c(z'))\). Since H10 and H11 apply to every
positive next state, no endpoint derivative enters this branch. The
canonical optimizer is locally interior because both savings and
consumption are positive. Fermat's theorem applied to the actual Bellman
objective gives the Euler equality.

\textbf{Boundary and integrability audit.}
\texttt{zeroRightMarginal\ :\ ENNReal} remains the sole value marginal
at zero resources and may be infinite. The public theorem exposes its
almost-everywhere finiteness under the particular conditional law before
calling \texttt{.toReal}; the associated real integrability certificate
is returned before the real integral inequality. In the equality branch,
positive savings rules out zero next resources pointwise and ordinary
marginal utility receives a separate \texttt{Integrable} certificate.

\textbf{Source correspondence.} A93 Appendix Proposition 2(c), printed
pp.~37-38 / PDF pp.~38-39, states the envelope/Euler inequality and
equality for positive next assets. A94 equations (5)-(7), printed
pp.~666-667 / PDF pp.~9-10, record the Bellman objective, shifted asset
policy, and next resource transition used here. Both exact page ranges
were hash-verified, rendered, and visually inspected. The conditional
integrability and explicit zero-state reconstruction are formal
qualifications supplied by this project rather than assumptions imported
from the papers.

\textbf{Audit result.} All six M03C exports pass
\texttt{assert\_no\_sorry}. Their printed transitive axioms are exactly
\texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}.
Kernel checking supports REVIEW\_READY only; no adequacy certification
or GREEN status is asserted.

\section{H13 --- Binding interval
exists}\label{h13-binding-interval-exists}

\textbf{Status:} GREEN. Independent Astra acceptance:
\texttt{reviews/m03d\_acceptance.md}.

\textbf{Scope:} core. \textbf{Milestone:} 03.

\textbf{Target declaration:}
\texttt{Aiyagari1994.binding\_interval\_exists}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Household/BorrowingThreshold.lean}.

\textbf{Mathematical contract.} Under beta*R\textless1 and either
e\_min\textgreater0 or finite U\_right\_prime(0), there exists
zHat\textgreater e\_min with A(z)=0 for every z in {[}e\_min,zHat{]}.

\textbf{Assumption profiles:} BASIC, SMOOTH, IMPATIENT, THRESHOLD. These
are branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H08, H10, H12. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 3 and the following
note, printed p.~38 / PDF p.~39; A94 threshold discussion, printed
p.~667 / PDF p.~10.

\textbf{Exact signature.} The formal endpoint is
\texttt{minimumEffectiveIncome\ m\ :\ Resources}, the effective income
at the lower labor endpoint. The contract theorem is

\begin{verbatim}
binding_interval_exists
  (m : HouseholdPrimitives) (hsmooth : UtilitySmooth m.utility)
  (hbetaR : m.beta * m.prices.grossReturn < 1)
  (hthreshold : Or (0 < minimumEffectiveIncome m)
    (utilityZeroRightMarginal m < OrderTop.top)) :
  Exists fun zHat : Resources =>
    And (minimumEffectiveIncome m < zHat)
      (forall z : Resources, minimumEffectiveIncome m <= z -> z <= zHat ->
        assetPolicy m z = 0)
\end{verbatim}

\textbf{Actual and transitive economic assumptions.}
\texttt{HouseholdPrimitives} supplies BASIC: bounded, continuous,
strictly increasing and strictly concave utility; positive beta below
one; a general probability law on fixed compact positive labor support;
positive wage and gross return; and nonnegative affine effective income.
\texttt{UtilitySmooth} supplies SMOOTH. The strict inequality
\texttt{beta*R\textless{}1} is IMPATIENT. THRESHOLD is the displayed
disjunction: either the derived minimum effective income is positive or
the separate utility endpoint marginal
\texttt{utilityZeroRightMarginal\ :\ ENNReal} is finite. There is no
curvature, density, atom, nondegeneracy, stationarity, compact asset
bound, consumption-positivity premise, or assumed policy threshold. H10
derives consumption positivity. H08 and H12 supply the marginal facts.

\textbf{Readable proof.} Let \texttt{e\_min} be minimum effective income
and let \texttt{Q} be its extended value marginal, using
\texttt{zeroRightMarginal} when \texttt{e\_min\ =\ 0} and the finite
positive-state marginal otherwise. If
\texttt{e\_min\ \textgreater{}\ 0}, H08 makes \texttt{Q} finite. If
\texttt{e\_min\ =\ 0} and the utility endpoint marginal is finite, H10's
finite-horizon Lipschitz construction proves the global value increment
bound. Applying that bound to every secant from zero gives
\texttt{zeroRightMarginal\ \textless{}=\ utilityZeroRightMarginal\ \textless{}\ infinity}.
Thus in either branch \texttt{0\ \textless{}\ Q\ \textless{}\ infinity}.

Suppose \texttt{A(z)\ \textgreater{}\ 0}. H10 makes current and next
consumption positive. H12 first proves the next marginal-utility
integrand is integrable and then gives the interior Euler equality.
Every next resource satisfies
\texttt{R*A(z)\ +\ e(l)\ \textgreater{}=\ e\_min}. H08's antitonicity
therefore bounds each next value marginal by \texttt{Q}. H11 identifies
these positive-state value marginals with marginal utility, so
integration and the Euler equality yield
\texttt{q(z)\ \textless{}=\ beta*R*Q}. No real integral is used until
H12's integrability certificate is available.

Since \texttt{0\ \textless{}\ beta*R\ \textless{}\ 1},
\texttt{beta*R*Q\ \textless{}\ Q}. When
\texttt{e\_min\ \textgreater{}\ 0}, H08's genuine right continuity gives
a right neighborhood of \texttt{e\_min} on which
\texttt{q(z)\ \textgreater{}\ beta*R*Q}. When \texttt{e\_min\ =\ 0},
H08's convergence of positive-state marginals to the separate finite
\texttt{zeroRightMarginal\ :\ ENNReal} gives the same strict inequality
on a punctured right neighborhood. Hence an interior choice is
impossible there. At \texttt{e\_min\ =\ 0}, \texttt{assetPolicy\_zero}
handles the endpoint. At \texttt{e\_min\ \textgreater{}\ 0}, the
interior upper bound applied at \texttt{e\_min} itself contradicts
\texttt{Q\ \textless{}=\ beta*R*Q}, so the endpoint also binds. Choosing
a smaller positive neighborhood endpoint produces the required
\texttt{zHat} and establishes \texttt{A(z)=0} throughout the closed
interval.

\textbf{Boundary and integrability audit.} The proof never evaluates
\texttt{rightMarginalValue\ m\ 0}.
\texttt{zeroRightMarginal\ :\ ENNReal} is the sole value marginal at
zero and may be infinite before the THRESHOLD branch proves it finite.
\texttt{utilityZeroRightMarginal} is a distinct utility endpoint object.
The only new economic real integral is inherited through H12 together
with its explicit integrability proof.

\textbf{Source correspondence.} A93 Appendix Proposition 3, printed
p.~38 / PDF p.~39, states the qualified threshold and argues from the
interior marginal equality, monotonicity, and
\texttt{beta*R\textless{}1}. A94 printed p.~667 / PDF p.~10 records the
shifted policy, transition, and threshold discussion. Both approved
pages were hash-verified, rendered, and visually inspected. The formal
proof reconstructs the endpoint finiteness and neighborhood argument; it
does not certify the unqualified Inada note following Proposition 3.

\textbf{Audit result.} Both M03D exports pass
\texttt{assert\_no\_sorry}. Their printed transitive axioms are exactly
\texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}.
Kernel checking supports REVIEW\_READY only; no adequacy certification
or GREEN status is asserted.

\section{H14 --- Zero income atom implies
nonbinding}\label{h14-zero-income-atom-implies-nonbinding}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} diagnostic.
\textbf{Milestone:} 03.

\textbf{Target declaration:}
\texttt{Aiyagari1994.zero\_income\_atom\_implies\_nonbinding}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Household/BorrowingThreshold.lean}.

\textbf{Mathematical contract.} If e\_min=0, U has Inada marginal at
zero, and Pr(e=0)\textgreater0, then A(z)\textgreater0 for every
z\textgreater0. No impatience assumption is needed for this sufficient
condition.

\textbf{Assumption profiles:} BASIC, SMOOTH, ATOM\_INADA. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H03, H04, H08. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 3 and the following
note, printed p.~38 / PDF p.~39; A94 threshold discussion, printed
p.~667 / PDF p.~10.

\textbf{Readable proof plan:} Architecture §5.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{D01 --- Inada without atom binding
example}\label{d01-inada-without-atom-binding-example}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} diagnostic.
\textbf{Milestone:} 03.

\textbf{Target declaration:}
\texttt{Aiyagari1994.inada\_without\_atom\_binding\_example}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Diagnostics/InadaCounterexample.lean}.

\textbf{Mathematical contract.} In the exact continuous-state
uniform-income model in EXACT\_DIAGNOSTIC, all core primitive conditions
hold and A(z)=0 whenever 0\textless z\textless=1/100, despite Inada and
e\_min=0.

\textbf{Assumption profiles:} EXACT\_DIAGNOSTIC. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H02, H03, H04. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 3 and the following
note, printed p.~38 / PDF p.~39; A94 threshold discussion, printed
p.~667 / PDF p.~10.

\textbf{Readable proof plan:} Architecture §5.3.

\textbf{Adequacy note.} A proposed correction to the unqualified note
after Proposition 3, not to Proposition 3 itself.

\section{D02 --- Marginal Utility ratio
bound}\label{d02-marginal-utility-ratio-bound}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
04.

\textbf{Target declaration:}
\texttt{Aiyagari1994.marginalUtility\_ratio\_bound}.\\
\textbf{Module:} \texttt{Aiyagari1994/Analysis/Curvature.lean}.

\textbf{Mathematical contract.} For a sufficiently large positive
integer m dominating eventual relative risk aversion,
U\_prime(c1)/U\_prime(c2)\textless=(c2/c1)\^{}m whenever
C0\textless=c1\textless=c2.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} SE77, A93.

\textbf{Source locator:} A93 Appendix Proposition 4, printed pp.~38-39 /
PDF pp.~39-40; SE77 Theorems 3.8-3.9, printed pp.~161-162 / PDF
pp.~11-12.

\textbf{Readable proof plan:} Architecture §6.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{D03 --- Uniform upper drift}\label{d03-uniform-upper-drift}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
04.

\textbf{Target declaration:}
\texttt{Aiyagari1994.uniform\_upper\_drift}.\\
\textbf{Module:} \texttt{Aiyagari1994/Household/UpperDrift.lean}.

\textbf{Mathematical contract.} On any specified LOCAL\_IMPATIENT
parameter neighborhood, construct a common finite B\textgreater=all
e\_max such that R*A\_theta(z)+e\_max\textless=z for z\textgreater=B,
and {[}e\_min,B{]} is forward invariant. Do not claim finite-time entry.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE,
LOCAL\_IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} H07, H08, H10, H12, D02. \textbf{Source keys:}
SE77, A93.

\textbf{Source locator:} A93 Appendix Proposition 4, printed pp.~38-39 /
PDF pp.~39-40; SE77 Theorems 3.8-3.9, printed pp.~161-162 / PDF
pp.~11-12.

\textbf{Readable proof plan:} Architecture §6.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{S01 --- Household Kernel feller
monotone}\label{s01-household-kernel-feller-monotone}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
05.

\textbf{Target declaration:}
\texttt{Aiyagari1994.householdKernel\_feller\_monotone}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/Kernel.lean}.

\textbf{Mathematical contract.} The pushforward of nu under
l↦R*A(z)+e(l) is a probability Markov kernel, is Feller, and preserves
stochastic order. Establish the kernel test-function integral formula.

\textbf{Assumption profiles:} BASIC. These are branch-sensitive context
tags; the completed signature must list the actual premises.

\textbf{Dependencies:} H04, H07. \textbf{Source keys:} A93, SLP89.

\textbf{Source locator:} A93 Appendix Proposition 5 and proof, printed
pp.~39-40 / PDF pp.~40-41; SLP89 Section 12.4. The explicit
primitive-to-crossing construction is reconstructed here.

\textbf{Readable proof plan:} Architecture §7.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{S02 --- Lower transition iterates
tendsto}\label{s02-lower-transition-iterates-tendsto}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
05.

\textbf{Target declaration:}
\texttt{Aiyagari1994.lower\_transition\_iterates\_tendsto}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/LowerTransition.lean}.

\textbf{Mathematical contract.} Under beta*R\textless1,
h\_min(e\_min)=e\_min, h\_min(z)e\_min, and h\_min iterated from any
finite upper bound decreases to e\_min.

\textbf{Assumption profiles:} BASIC, SMOOTH, IMPATIENT. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H04, H08, H12. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 5 and proof, printed
pp.~39-40 / PDF pp.~40-41; SLP89 Section 12.4. The explicit
primitive-to-crossing construction is reconstructed here.

\textbf{Readable proof plan:} Architecture §7.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{S03 --- Economic crossing
condition}\label{s03-economic-crossing-condition}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
05.

\textbf{Target declaration:}
\texttt{Aiyagari1994.economic\_crossing\_condition}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/Crossing.lean}.

\textbf{Mathematical contract.} On a compact invariant interval,
construct d in (e\_min,e\_max), N\textgreater=1, epsilon\textgreater0
with P\^{}N(e\_min,{[}d,B{]})\textgreater=epsilon and
P\^{}N(B,{[}e\_min,d{]})\textgreater=epsilon from endpoint-neighborhood
probabilities.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} D03, S01, S02. \textbf{Source keys:} A93, SLP89.

\textbf{Source locator:} A93 Appendix Proposition 5 and proof, printed
pp.~39-40 / PDF pp.~40-41; SLP89 Section 12.4. The explicit
primitive-to-crossing construction is reconstructed here.

\textbf{Readable proof plan:} Architecture §7.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{S04 --- Compact monotone feller
stability}\label{s04-compact-monotone-feller-stability}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
05.

\textbf{Target declaration:}
\texttt{Aiyagari1994.compact\_monotone\_feller\_stability}.\\
\textbf{Module:} \texttt{Aiyagari1994/Analysis/MonotoneFeller.lean}.

\textbf{Mathematical contract.} A monotone Feller kernel on a nonempty
compact real interval satisfying the common-horizon endpoint-crossing
condition has exactly one invariant probability law; every initial
probability law converges weakly to it. Prove endpoint invariant
existence and oscillation contraction.

\textbf{Assumption profiles:} Generic mathematical hypotheses stated in
the contract. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} SLP89.

\textbf{Source locator:} SLP89 Assumption 12.1, Lemma 12.11 and Theorem
12.12, printed pp.~381-383 / PDF pp.~391-393.

\textbf{Readable proof plan:} Architecture §7.2.

\textbf{Adequacy note.} Generic mathematical theorem. Its crossing
hypothesis is legitimate here; S05 must discharge it from S03.

\section{S05 --- Stationary Law exists unique
global}\label{s05-stationary-law-exists-unique-global}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
05.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationaryLaw\_exists\_unique\_global}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/GlobalStability.lean}.

\textbf{Mathematical contract.} For the core strictly impatient
household, construct one invariant probability law supported on
{[}e\_min,B{]}. It is the only invariant probability law on all NNReal,
and every initial probability law converges weakly to it.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} D03, S01, S03, S04. \textbf{Source keys:} A93,
SLP89.

\textbf{Source locator:} A93 Appendix Proposition 5 and proof, printed
pp.~39-40 / PDF pp.~40-41; SLP89 Section 12.4. The explicit
primitive-to-crossing construction is reconstructed here.

\textbf{Readable proof plan:} Architecture §7.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{S06 --- Stationary Law weakly
continuous}\label{s06-stationary-law-weakly-continuous}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
06.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationaryLaw\_weakly\_continuous}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Stationary/ParameterContinuity.lean}.

\textbf{Mathematical contract.} With fixed U, nu and beta, the canonical
invariant law is weakly continuous at every strictly impatient
normalized price vector. Prove that each subsequential limit is
invariant on a common compact state interval.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} H06, D03, S05. \textbf{Source keys:} A93, SLP89.

\textbf{Source locator:} A93 Appendix Proposition 5; SLP89 Theorem
12.13, printed pp.~384-385 / PDF pp.~394-395.

\textbf{Readable proof plan:} Architecture §8.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{A01 --- Resource asset labor law
bridge}\label{a01-resource-asset-labor-law-bridge}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
06.

\textbf{Target declaration:}
\texttt{Aiyagari1994.resource\_asset\_labor\_law\_bridge}.\\
\textbf{Module:} \texttt{Aiyagari1994/Aggregate/CrossSection.lean}.

\textbf{Mathematical contract.} If rho=(A-phi)\#pi is the net-asset law,
then current predetermined assets and independent labor have law rho×nu,
whose resource image is pi. Give both directions of the stationary
resource-law versus asset/labor-law correspondence.

\textbf{Assumption profiles:} BASIC, IID, IMPATIENT. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P01, S05. \textbf{Source keys:} A93, A94.

\textbf{Source locator:} A94 equation (8) and stationary aggregation
discussion, printed pp.~667-670 / PDF pp.~10-13; A93 Proposition 5 for
invariant-law dependence.

\textbf{Readable proof plan:} Architecture §8.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{A02 --- Stationary budget
identity}\label{a02-stationary-budget-identity}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
06.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationary\_budget\_identity}.\\
\textbf{Module:} \texttt{Aiyagari1994/Aggregate/AssetSupply.lean}.

\textbf{Mathematical contract.} The stationary resource, asset and
consumption integrals are finite; S=E\_pi A-phi and E\_pi c=r*S+w*E\_nu
l. Prove E\_pi z=R*E\_pi A+E e before using the identity.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} S05, A01. \textbf{Source keys:} A93, A94.

\textbf{Source locator:} A94 equation (8) and stationary aggregation
discussion, printed pp.~667-670 / PDF pp.~10-13; A93 Proposition 5 for
invariant-law dependence.

\textbf{Readable proof plan:} Architecture §8.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{A03 --- Stationary Asset Supply
continuous}\label{a03-stationary-asset-supply-continuous}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
06.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationaryAssetSupply\_continuous}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Aggregate/ParameterContinuity.lean}.

\textbf{Mathematical contract.} S(theta)=Integral(A\_theta-phi\_theta)
dpi\_theta is continuous throughout the strictly impatient admissible
parameter region. Use a common compact interval and uniform-on-compact
policy convergence.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, IMPATIENT. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} H06, D03, S06, A02. \textbf{Source keys:} A93,
C90.

\textbf{Source locator:} A94 equation (8) and stationary aggregation
discussion, printed pp.~667-670 / PDF pp.~10-13; A93 Proposition 5 for
invariant-law dependence.

\textbf{Readable proof plan:} Architecture §8.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{A04 --- Certainty stationary assets at
limit}\label{a04-certainty-stationary-assets-at-limit}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.certainty\_stationary\_assets\_at\_limit}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Equilibrium/CertaintyBenchmark.lean}.

\textbf{Mathematical contract.} For deterministic positive labor and
beta*R\textless1, the canonical resource kernel has the unique invariant
law concentrated at its constant effective income e\_bar. The stationary
asset policy is zero in shifted units, so stationary net assets equal
minus the appropriate certainty debt limit. Prove global weak
convergence by iterating the strictly decreasing deterministic
transition above e\_bar; no income nondegeneracy or curvature hypothesis
is needed.

\textbf{Assumption profiles:} BASIC, SMOOTH, IMPATIENT. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H04, H10, H12, S02. \textbf{Source keys:} A93,
A94.

\textbf{Source locator:} A94 mean-income certainty and
precautionary-assets discussion, printed pp.~669-670 / PDF pp.~12-13,
especially notes 22-23; deterministic benchmark on p.~671 / PDF p.~14.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} The certainty labor value is the mean of risky
labor. Its natural debt limit uses that mean, not the risky minimum.

\section{A05 --- Risky assets above certainty near
impatience}\label{a05-risky-assets-above-certainty-near-impatience}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.risky\_assets\_above\_certainty\_near\_impatience}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Aggregate/CertaintyComparison.lean}.

\textbf{Mathematical contract.} At fixed positive wages and the same
institutional cap, or in the two respective natural-limit families,
risky stationary mean assets are weakly above the mean-income certainty
level at every strictly impatient admissible rate. They are strictly
above it for all rates sufficiently close to lambda from below. The weak
bound follows from nonnegative shifted assets and the ordering of
effective debt limits; the strict near-boundary result follows from B02.
Do not assume convex marginal utility or assert a general ordering
across two risky distributions.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, IMPATIENT, FINITE\_CAP, NATURAL\_CAP. These are branch-sensitive
context tags; the completed signature must list the actual premises.

\textbf{Dependencies:} P02, A02, A04, B02. \textbf{Source keys:} A94.

\textbf{Source locator:} A94 mean-income certainty and
precautionary-assets discussion, printed pp.~669-670 / PDF pp.~12-13,
especially notes 22-23; deterministic benchmark on p.~671 / PDF p.~14.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} This contracts the qualified
precautionary-assets claim; it is not the deferred Sibley/Miller
aggregate risk-order question.

\section{N01 --- Stationary zero state marginal
resolved}\label{n01-stationary-zero-state-marginal-resolved}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07a.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationary\_zero\_state\_marginal\_resolved}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/ZeroState.lean}.

\textbf{Mathematical contract.} For any candidate invariant probability
law at arbitrary R\textgreater0, the value marginal is finite and
positive almost everywhere: if q(0)=infinity then pi\{0\}=0. Prove the
atom/no-atom cases and conditional marginal integrability before
real-valued integration.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H04, H08, H09, S01. \textbf{Source keys:} CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{N02 --- Stationary bounded jensen
equality}\label{n02-stationary-bounded-jensen-equality}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07a.

\textbf{Target declaration:}
\texttt{Aiyagari1994.stationary\_bounded\_jensen\_equality}.\\
\textbf{Module:} \texttt{Aiyagari1994/Analysis/BoundedJensen.lean}.

\textbf{Mathematical contract.} For positive finite measurable q with
q\textgreater=gamma*Pq and a stationary law, psi(q) with psi(x)=x/(1+x)
rules out gamma\textgreater1. At gamma=1 it implies q(next)=q(current)
almost surely. Require only pointwise conditional integrability, not
finite stationary E q.

\textbf{Assumption profiles:} Generic mathematical hypotheses stated in
the contract. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.2.

\textbf{Adequacy note.} Proposed reusable lemma. Prove strict Jensen
with the exact tangent-gap identity in architecture §9.2.

\section{N03 --- No invariant
supercritical}\label{n03-no-invariant-supercritical}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07a.

\textbf{Target declaration:}
\texttt{Aiyagari1994.no\_invariant\_supercritical}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/Supercritical.lean}.

\textbf{Mathematical contract.} When beta*R\textgreater1 and effective
income is nondegenerate, the canonical household kernel has no invariant
probability law on NNReal. No moment or bounded-support hypothesis is
allowed on that law.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H09, S01, N01, N02. \textbf{Source keys:} A93,
A94, CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{N04 --- Critical stationary consumption
constant}\label{n04-critical-stationary-consumption-constant}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07a.

\textbf{Target declaration:}
\texttt{Aiyagari1994.critical\_stationary\_consumption\_constant}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Stationary/CriticalConsumption.lean}.

\textbf{Mathematical contract.} Under beta*R=1, any putative stationary
law would imply equal consumption across every adjacent pair and every
finite stationary history. Include zero-consumption corners via equal
right value marginals.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID. These
are branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} H11, H09, N01, N02. \textbf{Source keys:} CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.3.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{N05 --- Two independent histories
contradiction}\label{n05-two-independent-histories-contradiction}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07b.

\textbf{Target declaration:}
\texttt{Aiyagari1994.two\_independent\_histories\_contradiction}.\\
\textbf{Module:} \texttt{Aiyagari1994/Analysis/TwoShockStrings.lean}.

\textbf{Mathematical contract.} For R\textgreater1, a state-dependent
consumption rule c(z), and resource recursion Z\_next=R*(Z-c(Z))+e\_next
with nonnegative resources and bounded nondegenerate iid income, a
stationary probability law under which consumption remains constant
along finite histories is impossible. Couple the same initial state with
two independent future shock strings and contradict a strictly positive
discounted-shock variance. No state moments are assumed.

\textbf{Assumption profiles:} NONDEGENERATE, IID. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.4.

\textbf{Adequacy note.} Proposed new finite-history proof construction;
not presented as a theorem copied from the source.

\section{N06 --- No invariant critical}\label{n06-no-invariant-critical}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07b.

\textbf{Target declaration:}
\texttt{Aiyagari1994.no\_invariant\_critical}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/Critical.lean}.

\textbf{Mathematical contract.} When beta*R=1 and effective income is
nondegenerate, the canonical household kernel has no invariant
probability law on NNReal, including laws of infinite first moment.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID. These
are branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} S01, N04, N05. \textbf{Source keys:} A93, A94,
CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{N07 --- No invariant at or above
impatience}\label{n07-no-invariant-at-or-above-impatience}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
07b.

\textbf{Target declaration:}
\texttt{Aiyagari1994.no\_invariant\_at\_or\_above\_impatience}.\\
\textbf{Module:} \texttt{Aiyagari1994/Stationary/NoInvariant.lean}.

\textbf{Mathematical contract.} For beta*R\textgreater=1 and
nondegenerate iid effective income, no invariant probability law exists
for the canonical household kernel.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID. These
are branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} N03, N06. \textbf{Source keys:} A93, A94, CW00.

\textbf{Source locator:} A94 printed p.~669 / PDF p.~12, notes 20-21,
distinguishes pathwise claims from the stationary implication. CW00
supplies background; the bounded-Jensen/two-string stationary proof here
is a new reconstruction, not a cited source theorem.

\textbf{Readable proof plan:} Architecture §9.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{B01 --- Tight kernel invariant
limit}\label{b01-tight-kernel-invariant-limit}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
08.

\textbf{Target declaration:}
\texttt{Aiyagari1994.tight\_kernel\_invariant\_limit}.\\
\textbf{Module:} \texttt{Aiyagari1994/Analysis/TightKernelLimit.lean}.

\textbf{Mathematical contract.} For Feller probability Markov kernels
P\_n and P on NNReal, suppose invariant probability laws mu\_n form a
tight family and converge weakly along a subsequence, and P\_n f
converges locally uniformly to P f for each bounded continuous f.~Then
the limit law is invariant for P. Prove the compact/tail split and use
continuity of P f when passing the fixed test through weak convergence.

\textbf{Assumption profiles:} Generic mathematical hypotheses stated in
the contract. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} SLP89.

\textbf{Source locator:} New generic tight-kernel limit lemma; related
to the compact parameter argument in SLP89 Theorem 12.13, but the
noncompact tail passage is proved here.

\textbf{Readable proof plan:} Architecture §10.1.

\textbf{Adequacy note.} Generic lemma, not a primitive closure
assumption; demonstrate hypotheses in B02.

\section{B02 --- Asset Supply tends To infinity at
impatience}\label{b02-asset-supply-tends-to-infinity-at-impatience}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
08.

\textbf{Target declaration:}
\texttt{Aiyagari1994.assetSupply\_tendsTo\_infinity\_at\_impatience}.\\
\textbf{Module:} \texttt{Aiyagari1994/Aggregate/UpperBoundary.lean}.

\textbf{Mathematical contract.} If r\_n\textless lambda, r\_n→lambda,
w\_n→w\_star\textgreater0 and phi\_n→finite phi\_star through admissible
prices, then S\_n→+infinity. Prove sequentially via bounded-mean
subsequence, tightness, B01 and N07, then translate to one-sided
filters.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID. These are branch-sensitive context tags; the completed signature
must list the actual premises.

\textbf{Dependencies:} H06, A02, B01, N07. \textbf{Source keys:} A94,
C90.

\textbf{Source locator:} C90 Proposition 2.4, printed p.~548 / PDF p.~7
(stated without proof); A94 note 19 and upper-boundary discussion. The
proof route in architecture Section 10.1 is a reconstruction.

\textbf{Readable proof plan:} Architecture §10.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{B03 --- Natural Asset Supply tends To neg
infinity}\label{b03-natural-asset-supply-tends-to-neg-infinity}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
08.

\textbf{Target declaration:}
\texttt{Aiyagari1994.naturalAssetSupply\_tendsTo\_neg\_infinity}.\\
\textbf{Module:} \texttt{Aiyagari1994/Aggregate/LowerBoundary.lean}.

\textbf{Mathematical contract.} Under the natural limit,
r\_n\textgreater0 tends to zero and w\_n→w0\textgreater0 imply
S\_n→-infinity. Uniformly bound shifted saving in normalized
effective-income coordinates before subtracting phi\_n→infinity.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, NATURAL\_CAP. These are branch-sensitive context tags; the
completed signature must list the actual premises.

\textbf{Dependencies:} D03, A02. \textbf{Source keys:} A93, A94, C90.

\textbf{Source locator:} A94 natural-limit boundary discussion, printed
p.~673 / PDF p.~16; C90 Proposition 2.4, printed p.~548 / PDF p.~7.

\textbf{Readable proof plan:} Architecture §10.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{F01 --- Capital Demand wage
constructed}\label{f01-capital-demand-wage-constructed}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.capitalDemand\_wage\_constructed}.\\
\textbf{Module:} \texttt{Aiyagari1994/Firms/Neoclassical.lean}.

\textbf{Mathematical contract.} For r\textgreater-delta, construct the
unique K(r)\textgreater0 satisfying f\_prime(K)=r+delta and
w(r)=f(K)-K*f\_prime(K)\textgreater0. Prove firm optimization,
continuity of both functions and strict decrease of K.

\textbf{Assumption profiles:} PRODUCTION. These are branch-sensitive
context tags; the completed signature must list the actual premises.

\textbf{Dependencies:} Primitive mathematics / installed Mathlib.
\textbf{Source keys:} A94.

\textbf{Source locator:} A94 firm-side general-equilibrium discussion,
printed pp.~670-671 / PDF pp.~13-14; explicit primitive assumptions and
consistency witness are supplied here.

\textbf{Readable proof plan:} Architecture §11.

\textbf{Adequacy note.} Also construct f(K)=sqrt(K), delta=1/2 as a
production witness and combine it with P03 to demonstrate nonempty full
equilibrium primitives.

\section{F02 --- Finite Cap lower
bracket}\label{f02-finite-cap-lower-bracket}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.finiteCap\_lower\_bracket}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/LowerBracket.lean}.

\textbf{Mathematical contract.} For every finite b\textgreater=0, derive
r\_L in (-delta,0) with stationary asset supply
S(r\_L,w(r\_L))\textless K(r\_L), using f(K)/K→0 and E
c=w+rS\textgreater=0. Do not assume an excess-supply sign.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, FINITE\_CAP, LABOR\_MEAN\_ONE, PRODUCTION. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P02, A02, F01. \textbf{Source keys:} A94.

\textbf{Source locator:} New derived lower-bracket lemma supporting A94
equilibrium existence; not a theorem explicitly proved by the source.

\textbf{Readable proof plan:} Architecture §11.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G01 --- Equilibrium resource asset
iff}\label{g01-equilibrium-resource-asset-iff}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.equilibrium\_resource\_asset\_iff}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/Definition.lean}.

\textbf{Mathematical contract.} Define stationary equilibrium with firm
optimization, canonical lifetime-optimal policy, invariant
cross-sectional law, integrability and market clearing. Prove
resource-law and net-asset/current-labor formulations equivalent. The
definition allows all r\textgreater-delta and does not require
r\textless lambda.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
LABOR\_MEAN\_ONE, PRODUCTION. These are branch-sensitive context tags;
the completed signature must list the actual premises.

\textbf{Dependencies:} P01, H05, S01, A01, F01. \textbf{Source keys:}
A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G02 --- Finite Cap equilibrium
exists}\label{g02-finite-cap-equilibrium-exists}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.finiteCap\_equilibrium\_exists}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/Existence.lean}.

\textbf{Mathematical contract.} For every finite b\textgreater=0 and the
core economic primitives, there exists a stationary equilibrium with r
in (-delta,lambda). Derive both signs from F02 and B02 and apply
continuity/IVT. Do not assert uniqueness or positive interest.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, FINITE\_CAP, LABOR\_MEAN\_ONE, PRODUCTION. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P02, A03, B02, F02, G01. \textbf{Source keys:}
A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G03 --- Natural Cap equilibrium
exists}\label{g03-natural-cap-equilibrium-exists}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.naturalCap\_equilibrium\_exists}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/Existence.lean}.

\textbf{Mathematical contract.} For the natural-limit model and the core
economic primitives, a stationary equilibrium exists with
0\textless r\textless lambda. Derive endpoint signs from B03 and B02
along the firm wage schedule.

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, NATURAL\_CAP, LABOR\_MEAN\_ONE, PRODUCTION. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P02, A03, B02, B03, F01, G01. \textbf{Source
keys:} A93, A94.

\textbf{Source locator:} A94 natural-limit existence discussion and note
30, printed p.~673 / PDF p.~16.

\textbf{Readable proof plan:} Architecture §11.3.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G04 --- Every equilibrium rate below
impatience}\label{g04-every-equilibrium-rate-below-impatience}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.every\_equilibrium\_rate\_below\_impatience}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/MainTheorem.lean}.

\textbf{Mathematical contract.} Every stationary equilibrium satisfying
the untruncated definition has r\textless lambda, because its invariant
law contradicts N07 if beta*(1+r)\textgreater=1. This is not restricted
to the particular equilibria constructed by G02/G03.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
LABOR\_MEAN\_ONE, PRODUCTION. These are branch-sensitive context tags;
the completed signature must list the actual premises.

\textbf{Dependencies:} N07, G01. \textbf{Source keys:} A93, A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G05 --- Certainty benchmark
verified}\label{g05-certainty-benchmark-verified}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.certainty\_benchmark\_verified}.\\
\textbf{Module:}
\texttt{Aiyagari1994/Equilibrium/CertaintyBenchmark.lean}.

\textbf{Mathematical contract.} Construct and verify the mean-income
certainty steady state r=lambda, K=K(lambda), positive stationary
consumption. Use utility concavity and the deterministic present-value
budget, not just an Euler equality.

\textbf{Assumption profiles:} BASIC, SMOOTH, LABOR\_MEAN\_ONE,
PRODUCTION. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} P01, H05, F01. \textbf{Source keys:} A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G06 --- Equilibrium capital above
certainty}\label{g06-equilibrium-capital-above-certainty}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.equilibrium\_capital\_above\_certainty}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/MainTheorem.lean}.

\textbf{Mathematical contract.} Every risky stationary equilibrium has
K\textgreater K\_FI=K(lambda).

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
LABOR\_MEAN\_ONE, PRODUCTION. These are branch-sensitive context tags;
the completed signature must list the actual premises.

\textbf{Dependencies:} F01, G04, G05. \textbf{Source keys:} A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G07 --- Equilibrium gross saving share above
certainty}\label{g07-equilibrium-gross-saving-share-above-certainty}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.equilibrium\_gross\_saving\_share\_above\_certainty}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/Saving.lean}.

\textbf{Mathematical contract.} Every risky stationary equilibrium has
delta*K/f(K)\textgreater delta*K\_FI/f(K\_FI). Prove that the gross
investment share is strictly increasing using
f(K)-K*f\_prime(K)\textgreater0. Net saving is not the outcome.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
LABOR\_MEAN\_ONE, PRODUCTION. These are branch-sensitive context tags;
the completed signature must list the actual premises.

\textbf{Dependencies:} F01, G06. \textbf{Source keys:} A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{G08 --- Equilibrium goods market
clears}\label{g08-equilibrium-goods-market-clears}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
09.

\textbf{Target declaration:}
\texttt{Aiyagari1994.equilibrium\_goods\_market\_clears}.\\
\textbf{Module:} \texttt{Aiyagari1994/Equilibrium/Saving.lean}.

\textbf{Mathematical contract.} At every stationary equilibrium, E c +
delta*K=f(K), deriving goods clearing from household stationarity and
competitive factor payments. Establish required moment statements for a
general equilibrium law.

\textbf{Assumption profiles:} BASIC, SMOOTH, NONDEGENERATE, IID,
LABOR\_MEAN\_ONE, PRODUCTION. These are branch-sensitive context tags;
the completed signature must list the actual premises.

\textbf{Dependencies:} G01, F01. \textbf{Source keys:} A94.

\textbf{Source locator:} A94 general-equilibrium and certainty
comparison, printed pp.~670-671 / PDF pp.~13-14, especially notes 24-27.

\textbf{Readable proof plan:} Architecture §11.4.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{NP01 --- Discounted budget
telescope}\label{np01-discounted-budget-telescope}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.discounted\_budget\_telescope}.\\
\textbf{Module:} \texttt{Aiyagari1994/Budget/NoPonzi.lean}.

\textbf{Mathematical contract.} For every finite T, sum(t=0..T)
R\^{}(-t)c\_t=R*a\_0+sum(t=0..T)
R\textsuperscript{(-t)w*l\_t-R}(-T)*a\_(T+1). Prove exactly the timing
and discount exponents used by the source.

\textbf{Assumption profiles:} BASIC, PATH\_FEASIBILITY. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P01. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 1, printed p.~37 / PDF
p.~38; A94 p.~666 / PDF p.~9, note 16.

\textbf{Readable proof plan:} Architecture §12.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{NP02 --- Natural Bound implies no
Ponzi}\label{np02-natural-bound-implies-no-ponzi}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.naturalBound\_implies\_noPonzi}.\\
\textbf{Module:} \texttt{Aiyagari1994/Budget/NoPonzi.lean}.

\textbf{Mathematical contract.} For r\textgreater0, bounded income and
the natural asset floor imply existence of a finite nonnegative
discounted terminal wealth limit, almost surely. Do not strengthen
nonnegative to zero.

\textbf{Assumption profiles:} BASIC, PATH\_FEASIBILITY, NATURAL\_CAP.
These are branch-sensitive context tags; the completed signature must
list the actual premises.

\textbf{Dependencies:} NP01. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 1, printed p.~37 / PDF
p.~38; A94 p.~666 / PDF p.~9, note 16.

\textbf{Readable proof plan:} Architecture §12.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{NP03 --- No Ponzi implies natural
Bound}\label{np03-no-ponzi-implies-natural-bound}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} core. \textbf{Milestone:}
10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.noPonzi\_implies\_naturalBound}.\\
\textbf{Module:} \texttt{Aiyagari1994/Budget/NoPonzi.lean}.

\textbf{Mathematical contract.} For r\textgreater0 and an adapted
feasible plan, existence of a finite nonnegative discounted terminal
wealth limit almost surely implies the natural debt floor at every date
almost surely. Use a positive-probability debt violation and a finite
low-income history.

\textbf{Assumption profiles:} BASIC, NONDEGENERATE, IID,
PATH\_FEASIBILITY. These are branch-sensitive context tags; the
completed signature must list the actual premises.

\textbf{Dependencies:} NP01. \textbf{Source keys:} A93.

\textbf{Source locator:} A93 Appendix Proposition 1, printed p.~37 / PDF
p.~38; A94 p.~666 / PDF p.~9, note 16.

\textbf{Readable proof plan:} Architecture §12.1.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{E01 --- Zero Rate cap change
exact}\label{e01-zero-rate-cap-change-exact}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} extension.
\textbf{Milestone:} 10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.zeroRate\_cap\_change\_exact}.\\
\textbf{Module:} \texttt{Aiyagari1994/Extensions/BorrowingCap.lean}.

\textbf{Mathematical contract.} At r=0 and fixed w, changing b from b1
to b2 does not change the shifted problem or its canonical policy/law
and changes stationary net asset supply by -(b2-b1).

\textbf{Assumption profiles:} BASIC, SMOOTH, CURVATURE, NONDEGENERATE,
IID, FINITE\_CAP. These are branch-sensitive context tags; the completed
signature must list the actual premises.

\textbf{Dependencies:} P02, H02, H04, S05, A02. \textbf{Source keys:}
A93, A94.

\textbf{Source locator:} A94 borrowing-limit discussion, printed
pp.~672-673 / PDF pp.~15-16, and equation (2) on printed p.~666 / PDF
p.~9.

\textbf{Readable proof plan:} Architecture §12.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{E02 --- Caps above natural Limit
equivalent}\label{e02-caps-above-natural-limit-equivalent}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} extension.
\textbf{Milestone:} 10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.caps\_above\_naturalLimit\_equivalent}.\\
\textbf{Module:} \texttt{Aiyagari1994/Extensions/BorrowingCap.lean}.

\textbf{Mathematical contract.} At r\textgreater0, any two finite caps
weakly above w*l\_min/r induce identical effective household primitives,
policies and, when subcritical, stationary laws and net asset supply.

\textbf{Assumption profiles:} BASIC, FINITE\_CAP. These are
branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P02, H02, H04, S05. \textbf{Source keys:} A93,
A94.

\textbf{Source locator:} A94 borrowing-limit discussion, printed
pp.~672-673 / PDF pp.~15-16, and equation (2) on printed p.~666 / PDF
p.~9.

\textbf{Readable proof plan:} Architecture §12.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\section{E03 --- Natural Bound government Debt
correspondence}\label{e03-natural-bound-government-debt-correspondence}

\textbf{Status:} UNFORMALIZED. \textbf{Scope:} extension.
\textbf{Milestone:} 10.

\textbf{Target declaration:}
\texttt{Aiyagari1994.naturalBound\_governmentDebt\_correspondence}.\\
\textbf{Module:} \texttt{Aiyagari1994/Extensions/GovernmentDebt.lean}.

\textbf{Mathematical contract.} In pure exchange under tax-adjusted
natural borrowing, x=a-d maps budget, admissibility, lifetime
optimality, invariant laws and bond clearing bijectively between
government-debt and debt-free economies. It is not a statement about
capital demand or a fixed borrowing cap.

\textbf{Assumption profiles:} BASIC, IID, PURE\_EXCHANGE\_DEBT. These
are branch-sensitive context tags; the completed signature must list the
actual premises.

\textbf{Dependencies:} P01, H05. \textbf{Source keys:} A93, A94.

\textbf{Source locator:} A94 government-debt reinterpretation, printed
pp.~673-674 / PDF pp.~16-17.

\textbf{Readable proof plan:} Architecture §12.2.

\textbf{Adequacy note.} No proof or adequacy certification is asserted
by this initial entry.

\end{document}
